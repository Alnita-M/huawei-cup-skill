# 华为杯解题 Skill · 变更日志

> 版本路径：v0.1（蒸馏完成）→ v0.9（盲测通过）→ v1.0（实战评估后终版）→ v1.1（方法内核+工程外壳）。每次修订在此记录。

## v0.1 —— 2026-09-06（蒸馏完成，待盲测）

**原料**：2017–2021 华为杯 11 题（D01–D11）的「盲做 → 对照 → 差距分析」三件套。图像视觉类：2017D/2019B/2019C/2020E(前段)；数据预测类：2018D/2019D/2019E/2020B/2020C/2021B/2021D/2020E(后段)；混合类：2020E。

**产出**（skill/ 目录）：
- `SKILL.md`（32KB，230 行）：适用范围与边界、题目分类器、双方法论决策树（图像：预处理→特征→模型→评估；预测：数据审计→特征工程→模型选择→验证→不确定性分析，约 40 个「条件→选择→理由→溯源」决策点）、顶层 6 步正向推导流程、配套文件索引；全部规则带 [D01–D11] 溯源。
- `陷阱库.md`（17KB，204 行）：30 条陷阱（11 题全覆盖），每条「陷阱→后果→规避→溯源」。
- `论文报告结构模板.md`（7.6KB）：按华为杯评阅习惯七章结构 + 24 项提交前可勾选自检清单。
- `templates/`（6 个 .py + README.md，共约 60 个函数骨架）：image_preprocess / image_features_modeling / predictor_data_audit / predictor_feature_engineering / predictor_modeling_validation / uncertainty_optimization。

**已吸收的跨题杀手锏**：混叠优先原则（D02）、观测数阈值闭环（D02）、误差源清单决定构型结论（D03）、参考文献列表即泄题信号（D01）、题面数字即官方参数（D05）、对比实验是得分点（D01/D05）、概念→可计算指标→预测三层闭环（D06）、同构不同参不混训（D10）、逐时预测再聚合（D10）、AQI 锚点分季聚类（D10）、DEA-BCC 理想样本标杆+MIP（D07）、暗通道先验+导向滤波（D09）、光流测速→时间反推（D04）、代理模型+启发式+数据范围定界+第二方法复核（D11）、GBDT/1D-CNN 为精度主力（D08/D07）、被测量不做常识硬约束（D04）、精度主导项判别与 3σ（D07）、残差学习（D10）、克里金各向异性插值（D02）等。

**已知边界**：只覆盖图像视觉类与数据预测类；纯运筹/纯物理推导/纯定性题不适用（SKILL.md 已声明）。原料中「待核实/中可信度」细节（如 D03 角距截断、D07 99.6%）仅作量级参考。

**下一步**：阶段四盲测闸门（用本 Skill 解 2022A/2023D/2023E/2023F 四题，对齐度 ≥70% 则升 v0.9）。

---

## v0.9 —— 2026-09-06（盲测通过版）

**盲测闸门**：用 v0.1 解 2022A（信号/阵列超分辨定位）、2023D（区域双碳路径规划）、2023E（脑卒中临床诊疗）、2023F（强对流降水临近预报）四题（禁网、未读参考解析、不编造数值）。逐小问对齐度：**H01=87.5%（[3+0.5×1]/4）、H02=81.25%（[5+0.5×3]/8）、H03=77.8%（[5+0.5×4]/9）、H04=87.5%（[3+0.5×1]/4），全部 ≥70% → GATE: PASS**。修订依据：四份盲测解答第七章「试用反馈」（好使处/缺指引/冲突）+ 四份比对报告「偏离点细目」。

**新增决策点**（16 个条目，全部带 [H01]–[H04] 溯源）：
- 3.1 新增 **STEP B′ 信号/阵列谱估计层**：S1 MDL/AIC/特征值 gap 定阶、S2 MUSIC/ESPRIT/root-MUSIC 子空间超分辨、S3 相干信号解相干（前后向平滑/Toeplitz–MTOEP）、S4 弱目标稀疏重构 CS/SBL、S5 阵列自校准 ALS（[H01]）；
- 3.1 新增 **STEP R 栅格场/图像序列时空预报层**：R1 光流/物理外推 vs 深度时空（U-Net/ConvLSTM）双轨、R2 生成式（cGAN）缓解回归到平均、R3 预报场评估指标库 CSI/POD/FAR/Bias/SSIM+分层（[H04]）；
- 3.2 特征工程新增 **F9 指标体系构建**（政策锚点驱动/可计算/口径可核对）、**F10 指数分解**（LMDI 加和+残差分配+ANOVA/灰色关联工具选型）（[H02]）；
- 3.2 模型选择新增 **M9 情景-核算-目标判定闭环**（情景参数化→核算矩阵勾稽→目标对标+灵敏度）、**M10 重复测量/纵向数据**（LME 随机截距斜率+对象级 CV+轨迹形态聚类）、**M11 有序分类**（累积链接+QWK）、**M12 观察性混杂控制**（倾向评分/IPW/FDR，因果方向显式假设）、**M13 综合评价方法库**（AHP/熵权/TOPSIS）（[H02][H03][H04]）；
- 3.3 新增**机理公式+数据驱动双轨衔接**决策点（先问『有无可用机理公式』→ 显式物理反演 vs 物理衍生场+网络内嵌；机理作基线/初值/软约束）（[H04]）。

**冲突/歧义修正**（6 处，均加适用前提或改写）：
1. A2『带通滤波+EMD/DWT』加前提：仅适用生理信号等非距离编码信号；频率即信息量的信号（FMCW IF）禁用滤波去噪（[H01]）；
2. B3『先测速再测距』注明仅适用单目视觉场景，不普适（[H01]）；
3. M4『逐对象独立训练』补充：深度学习池化训练时『对象/样本』语义需按赛题重新界定（[H04]）；
4. F1 红线加适用前提：『设计指标/构造指标』类小问不适用（[H02]）；
5. 分类器动词表补『描述分析/综合评价/政策评估』类动词并给出路（[H02][H03]）；
6. 陷阱库 T17 补盲测说明：机理先验可作因果方向/结构约束；**聚类事实优先，机理用于解释、不用于否定结构**（[H03]）。

**陷阱库**：30 → **42 条**（新增 T31–T42，每条「陷阱→后果→规避→溯源[H0x]」；T11/T27/T28/T29 同步补适用前提迁移注记）。

**成绩对照**：H01=87.5%（主缺口：谱估计子树+相干处理，已补 S1–S5）；H02=81.25%（主缺口：指标体系/指数分解/情景闭环，已补 F9/F10/M9）；H03=77.8%（主缺口：重复测量/有序分类/混杂控制，已补 M10–M12）；H04=87.5%（主缺口：场序列时空预测/机理双轨/综合评价，已补 R1–R3/3.3 衔接/M13）。

**下一步**：v1.0 实战评估（正式赛题实战）后修订；templates/ 待补信号谱估计（spectral_estimation.py）、核算台账（emissions_ledger.py）与情景规划（scenario_planner.py）骨架。
---

## v1.0 —— 2026-09-06（终版：实战评估合入）

**实战评估**：用 v0.9 禁网独立解答 2024C/2024D/2025C/2025D 四题（零编造、【不确定】纪律），由独立评估代理联网比对获奖/权威方案。四维评分（对齐/偏差/合理/规范）：**F01 2024C=82/90/88/85**、**F02 2024D=84/87/86/86**；2025 两题按 0–1 制：**F03 2025C 总评≈0.80**（S1 0.72/S2 0.95/S3 0.78/S4 0.82）、**F04 2025D 总评≈0.84**（S1 0.79/S2 0.95/S3 0.80/S4 0.85）。两题共通最高优先级缺口=『无附件不交付数值=系统性失分』（F03/F04）。

**评测协议备注（证据降级声明）**：2024/2025 届获奖优秀论文全文均未公开可读（官网仅公布获奖名单、zhanwen/MathModel 未收录 2025 届、2024 届百度云/官方平台需登录），获奖侧对齐判定基于高校官方披露的方案要点（如 2024C 冠军『双向 LSTM+理想点 NSGA』、一等奖『温度修正 SE+改进多目标优化』）+ 多源系统性解析，按『部分证据』降级处理；四题零编造与全部门槛级硬口径核验（附件结构/样本序号/编码/小数位/年份范围等，经官方原题与原始数据实测）为全证据链最高可信部分。修订仅采纳评估报告『Skill 修订建议清单』中由实战+比对证据直接驱动的条目。

**新增决策点**（全部带 [F01]–[F04] 溯源）：
1. 3.1 STEP C 新增 **C4 标注预算评估**：图像数<50 或单图标注<1h → 默认人工标注部分图+监督学习（半监督可加），合成/弱标签降级为增强手段（[F03]）；
2. 3.1 STEP R 新增 **R4 时空场时间维平滑**（一阶卡尔曼递推/指数平滑+半拉格朗日/光流平流一致性检查；融合题默认『空间+时间』双平滑）与 **R5 集合预报形态**（多初值/多模型/幅度 ±10–20%、方向 ±10° 扰动→P10/P50/P90 分位聚合，输出形态优先集合）（[F04]）；
3. 3.2 M1 新增 **序列深度模型双轨**：数值序列+时间结构→LSTM/BiLSTM/GRU/CNN-LSTM 具名入主线候选与树模型并列裸比，1D-CNN 仅通用对照（[F01]）；
4. 3.2 M5 新增 **多法并列融合决策**：同族≥3/三法并列→默认『多指标综合+主法标注』（加权最小二乘/OI≈3DVAR+光滑先验+一阶卡尔曼），单法深挖仅当标定可行（[F04]）；
5. 顶层新增 **第 0.5 步『数据形态直读』**（多数据集/多附件强制：类型体系/像素含义/年份截尾/缺测坐标，说明文件优先于常识猜测；比例场 dominant 化/比例转移两口径）与第 1 步 **『公式图片 OCR 复原优先』**（复原失败才走通行公式+常数参数化+【不确定】）（[F02][F03]）；
6. 第 5 步验证层新增 **【数值强制交付】**（v1.0 必改）：附件缺失/禁网→用题面规格合成数据跑通全流程，交付带真实数字的结果表/误差表/参数表/图/敏感性曲线并声明合成口径，禁止『预期实现结构』充当全部结果（[F03][F04]）。

**冲突/歧义修正**：无（四题实战未发现与既有决策点冲突的条目；F 系列全部为新增决策点与新增陷阱，未改写 H 系列）。

**陷阱库**：42 → **48 条**（新增 T43–T48：B_m 峰值口径 max|B| [F01]、多数据集不读说明文件=比例场按分类场 [F02]、无附件不交付数值=系统性失分 [F03][F04]、标注预算低估 [F03]、公式图片不先 OCR 复原 [F03]、多法并列不做综合 [F04]）。

**模板**：templates/ 新增第 11 个 **`ensemble_forecast.py`**（集合预报骨架：多成员生成/扰动、分位聚合 P10/P50/P90、区间输出、集合可靠性检验、平流一致性检查，[F04]）；templates/README.md 索引、管线建议与跨题复用要点同步（十一模板）。

**成绩对照**：F01=82/90/88/85（主缺口：Q4 序列深度模型主线席位，已补 M1 双轨）；F02=84/87/86/86（主缺口：数据形态直读，已补第 0.5 步）；F03≈0.80（主缺口：无数值交付+标注预算+公式复原，已补 C4/公式复原/第 5 步）；F04≈0.84（主缺口：无数值交付+多法综合+时间维+集合形态，已补 M5/R4/R5/第 5 步）。

**下一步**：v1.0 为终版，进入实际参赛使用；建议按评测协议备注在『数据附件可读』条件下回填四题数值重验（F01/F02 报告 P5 类备注项）。

---

## v1.1 —— 2026-09-06（方法内核+工程外壳）

**动机**：2025C 围岩裂隙同题对比实验（2026-09-06，同一题面/同一禁网条件/同一模型基底：mathodology（流程编排型 skill 包）试用 vs 本 Skill v1.0 实战）——解题方法层高度同源、质量相当（方法族/公式/先验全来自模型领域能力），差异集中在流程层；mathodology 流程层机制实证带来 3 个真实边际收益：① MECH 台账抓漏『卷绕同一性合并』（我方实战漏检，真实丢分风险）[C]；② 数学规格深度优先促成 Z2 噪声偏差解析式推导（比我们深）[C]；③ 三盲评 scorecard 逼出苛刻自评（三席 weighted_total 均 <58）[C]。定位升级为**方法内核+工程外壳**。

**吸收清单**（对比报告第四节 A 节，全部落实；[M]=吸收自 mathodology 机制（精简适配为本项目适用形态）、[C]=对比实验实证）：
1. **MECH 机制台账 + INN 创新台账**（[M]）：SKILL.md 第 1 步内嵌——开题逐句扫题面登记题面点名机制（建模/代理[给代理方案]/降级[给理由] 三态+高风险标注）与真实创新候选（≥1 项，禁止『合成数据还原生成族』/教科书做法冒充，无候选则显式标注获奖天花板风险）；第 7 步收尾逐条收口 modeled/descoped；
2. **工程闸门**（[M]）：问题理解→建模→求解验证→写作四阶段 gate；严重度阶梯 blocker/high/medium/low，blocker/high 未决不放行；每 gate 修复 ≤2 轮、整轮 ≤8 轮；预算耗尽输出 decision_memo（2–3 个选项+后果+推荐）停等人工；
3. **三盲评自评协议**（[M]）：交稿前三席盲评（A 综合[综述/建模/结果]、B 创新[创新/证据/写作]、C 怀疑派[正确性/复现性]）+ 每席 fix_one_thing；共享项分歧 >20 复查不平均；档位阈值参考锚点（国一 85/70、国一边缘 80/65、国二 75/60，参考锚点非承诺）；
4. **图表证据角色表**（[M]）：论文模板新增第 8 节——每图登记七类证据角色之一（问题/数据、模型结构、核心结果、比较/消融、敏感性/鲁棒性、机制解释、验证/真值对照），禁装饰图，无角色图删除或转附录；
5. **诚实性机制化**（[M]）：第 5 步新增——by-construction 标注、headline-robustness 头条数字压力测试、CRN 共用随机数、概率结论报 MC 标准误、未核实信息（页码/卷期/版本）不打印；
6. **continuation state**（[M]）：第 7 步新增轻量格式——每轮结束输出 当前阶段/已过闸门/待决问题/假设清单/产物路径/下一步，多会话续跑免重读历史。

**陷阱库**：48 → **50 条**（新增 T49 题面机制 silent descope [M][C]、T50 装饰性图表 [M]，见『流程层陷阱』节）。

**论文模板**：新增第 8 节《图表证据角色表》与第 9 节《评审模拟 scorecard》（含 fix_one_thing 栏与分歧>20 复查规则）；自检清单尾部追加 v1.1 工程外壳 3 项勾选；既有 24 项原样保留。

**保留内容声明**：v1.0 全部内容未动——题目分类器、56 决策点决策树、48 条既有陷阱、11 个代码模板、D/H/F 三系溯源体系、论文模板既有章节与 24 项自检清单，全部原样保留；本版为纯增补（新增字段/新增章节/新增条目），无删除、无弱化。

**下一步**：进入实际参赛使用；建议在真实赛程中验证工程闸门与台账收口的成本收益，并回填 2025C 同题对比经验（卷绕合并、Z2 噪声推导）到对应决策点。

---

## v1.2 —— 2026-09-10（实测实证层 [E]）

**动机**：Skill 此前全部证据来自『盲做→对照』与『同题对比』（文本层证据），从未在**真实公开数据**上跑通完整交付链（题目 → 数据 → 训练 → 评估 → 报告 → Word）。本次 A 方案补上这一环：crack-seg 4029 张裂缝图（train 3717/val 200/test 112，416×416，YOLO 多边形标注，124 MB，gh-proxy 镜像获取）实跑 2025C 风格裂缝分割基线——轻量 U-Net 从头训练 **23.1 分钟**，test **Dice 0.7377 / IoU 0.5844 / P 0.7399 / R 0.7354**，峰值显存 2074 MB。实测暴露 3 个流程缺口（标注转换口径、评估指标口径、逐图分布缺失），全部固化为规则；**并且新模板交叉校验直接抓出实测脚本自身的 PA 公式口径错误（0.6566 → 正确 0.9899）**，反向印证『公式与聚合层级必须写清』这条规则的必要性。

**吸收清单**（实测报告《2025C_Q1实测报告_crack-seg_U-Net》第 5 节 5 条反哺建议，全部落实；[E]=真实数据实跑实证）：
1. **STEP E 分割/像素级任务专层**（[E]）：SKILL.md 第三节 3.1 新增 E1–E5——E1 标注格式转 mask 口径（双口径 + 正样本像素占比一致性检查）、E2 评估口径（Dice/IoU 为主、PA 仅辅证、必须声明批级/逐图）、E3 逐图分布 + 最差样本三联图归因、E4 从头训练收益天花板与 early stop patience=8、E5 基线参考值（416px / 1.94M 参数 → Dice 0.7377）+ 六级升级清单；理由=原决策树有预处理/特征建模/评估层但无**像素级分割任务专层**，细粒度口径类陷阱无处落位；
2. **陷阱库 +3 条**（[E]）：T51 标注转换不声明口径、T52 评估指标口径混用（PA 当主指标/聚合层级不声明）、T53 只报均值不报分布与最差样本；
3. **2 个可运行模板**（[E]）：`templates/polygon_to_mask.py`（YOLO/COCO 多边形→二值 mask 双口径 + 正样本占比/mask 覆盖率一致性检查）、`templates/segmentation_eval.py`（逐图 Dice/IoU/P/R + 分布分位数 + 批级/逐图双口径 + 最差样本三联图导出）；均取自实测脚本 `train_crackseg.py` 中已跑通的实现；
4. **论文模板**：评估与验证章节追加『逐图指标分布 + 最差样本归因』强制项，与第 8 节图表证据角色表的『验证/真值对照』角色对齐。

**实测数据存档**（供 B 方案对照）：产物 `G:\AgentZone\crackseg_unet`（train_crackseg.py 236 行、best.pt、run.log、curve.png、test_metrics.json、vis\ 6 张）；数据 `G:\AgentZone\datasets\crack-seg\`；报告 `C:\Users\18107\Desktop\Hermes Work\对比实验\2025C_Q1实测报告_crack-seg_U-Net.md` + Word 版 + `实测图表_crackseg\`。收敛轨迹：ep1 0.676 → ep3 0.736 → ep15 峰值 0.753 → early stop @ep23（no improve 8）。**双口径评估结果**：批级 Dice 0.7377 / IoU 0.5844 / P 0.7399 / R 0.7354 / PA 0.9899；逐图 Dice 0.7196 ± 0.1668（P10 0.586 / P50 0.756 / P90 0.863），6/112 张 <0.4、最差 2 张 0.000（完全漏检）——逐图与批级差 1.8 个百分点，最差样本全被均值掩盖（这两条正是 E2/E3 的实证来源）。逐图评估产物 `G:\AgentZone\crackseg_unet\per_image_eval\`（metrics_per_image.json + 5 张三联图）。

**保留内容声明**：v1.1 全部内容未动——题目分类器、双方法论决策树全部决策点、50 条既有陷阱、11 个既有模板、D/H/F/M/C 溯源体系、论文模板既有章节与自检清单，全部原样保留；本版为纯增补。

**下一步**：B 方案对照基线（判明 0.7377 处于什么水平）——候选：预训练编码器 / TTA / 更强增强与损失 / 深监督；域差距（混凝土·石材 ≠ 岩体钻孔裂隙）待更贴题数据可得后回填。

**v1.2 补记（同轮完成）**：B 方案四组对照全部跑完（同 test 112 张 / 阈值 0.5 / 同评估实现）：预训练编码器 **+1.59 pt**（12.1 分钟，比基线还快 11 分钟）、TTA 全增强 **+2.45 pt**（零训练成本）、二者组合 **Dice 0.7721 / IoU 0.6288（+3.45 pt）**，且尾部失效减半（<0.4 从 6/112 降到 3/112）；**强数据增强为负收益**（两轮实测 0.7034 / 0.7049 / 0.7216 均低于基线 0.7377）。另用外部路面裂缝数据集做了跨域实测：同域 0.7377 → CFD 跨域 **0.5177**（A0）/ **0.5676**（B3），**量化了域偏移代价 ≈ 18–22 个 Dice 点**，预训练编码器的跨域鲁棒性显著更好（崩坏样本 26→9 张）。产物 `G:\AgentZone\crackseg_unet`（bench_unet.py / tta_eval.py / dist_eval.py / cross_summary.py）。

---

## v1.3 —— 2026-09-10（无标注实战层 [G]：官方附件真实数据）

**动机**：拿到 **2025C 官方附件**（此前只能用 crack-seg 替代数据，存在域差距）后，用 Skill 正向流程实做**问题 1：裂隙像素智能识别**——附件 1 十张钻孔展开图（244×1350，**无任何标注**），题目要求输出同尺寸二值图（裂隙=黑/其余=白）。这是 Skill 首次面对『**有真实数据但无真值**』的任务形态：既有 STEP E（有标注监督分割）不覆盖，D1 三层验证也不足以驱动迭代。

**实测结论**：建立『光照归一化 → 双向多尺度 Frangi（暗脊+亮脊）→ 暗带加权 → **正弦参数化 Hough 搜索**（题面公式 y=R·sin(2πx/P+β)+C）→ 迭代贪心+邻域抑制 → 图内自适应阈值 → 边界约束 → 响应自适应掩码』流程，共输出 **70 条裂隙曲线**及四参数（R/P/β/C），逐图复核显示主要裂隙带均被检出（抽验图1-2：10 条中约 6 条为独立真裂隙，原图约 8 条 → 精度约 60%、召回约 75%，属无监督方法正常水平，弱对比裂隙存在漏检）。物理标定 **0.3863 mm/px**（244 px = π×30 mm），题面『张开>1mm』⇔ ≥2.59 px。

**吸收清单**（4 条实测缺口 → 1 个新层 + 1 个新模板 + 4 条新陷阱，[G]=官方附件实战实证）：
1. **STEP G 无标注/弱标注图像任务实战层**（[G]）：G1 物理标定先行、G2 无标注任务以题面公式为主模型做参数化搜索、G3 参数空间去重失效改用成像空间邻域抑制、G4 逐图归一化响应必须用图内自适应阈值（D3 的跨图推广）、G5 无标注+无参考解析时视觉复核即验证通道且必须驱动迭代；
2. **陷阱库 +4 条**（[G]）：T54 未做物理标定就调参、T55 参数空间去重替代成像空间重叠判据、T56 逐图归一化后仍用固定阈值（批量漏检极隐蔽）、T57 无标注任务忽略题面公式；
3. **1 个可运行模板**（[G]）：`templates/sinusoid_hough.py`（参数化曲线 Hough 搜索 + 迭代邻域抑制 + 图内自适应阈值 + 边界约束 + 参数表导出），取自本次已跑通实现（`G:\AgentZone\huawei2025c\q1_sine_hough.py`）；
4. **验证方法补强**：D1 三层之外，真实赛题数据下新增『逐图视觉复核 → 缺陷归类 → 规则化修改』闭环（本次 v1→v5 五版迭代的全部缺陷均由复核指出并转化为规则/陷阱）。

**实测数据存档**：产物 `G:\AgentZone\huawei2025c`（q1_explore.py / q1_sine_hough.py / q1_render.py / build_q1_docx.py；结果 work\final\ 10 张 result+vis+tri、参数 work\v5_hough\fracture_params.json）；数据 `G:\AgentZone\huawei2025c\data\附件1-4`（官方附件，71 张）；报告 `C:\Users\18107\Desktop\Hermes Work\2025C_问题1_实现报告_裂隙像素识别.docx`（2.2 MB，5 表 3 图，三重校验 ok）。

**保留内容声明**：v1.2 全部内容未动——题目分类器、双方法论决策树全部决策点（含 STEP E）、53 条既有陷阱、13 个既有模板、D/H/F/M/C/E 溯源体系、论文模板既有章节与自检清单，全部原样保留；本版为纯增补。

**下一步**：问题 2（正弦裂隙自动聚类与 R/P/β/C 表征——可直接复用本次 70 条参数与 G3 的聚类思路）、问题 3（JRC 粗糙度：轮廓线离散化 + 采样方式对结果的影响）、问题 4（多钻孔连通性 + 三维重构 + 补孔选址）。


---

## v1.4 —— 2026-09-10（一致性修订版：文档与内容对齐）

**动机**：v1.2 发布后由外部复核（Codex/ChatGPT 侧，基线 HEAD `5b7c8f4`）产出《华为杯Skill v1.3 修改清单》，发现文档层与内容层脱节：README 仍停留 v1.0（版本号/计数/步骤/安装命令全错，且引用了不存在的 `codex skill add` 子命令）、四个文件版本号各说各话（v1.2 / v1.2 / v0.1 / v1.2）、模板里残留被自家规则推翻的 PA 数字、分类器未索引 v0.9 之后新增的决策层、正文硬编码作者本机路径。**本版为纯一致性修订：不新增方法论结论、不改任何决策点与陷阱规则**（版本号合并说明：原计划的『v1.3 一致性修订版』因 v1.3 已被无标注实战层占用，统一顺延为 v1.4）。

**修订清单（C1–C16，全部落实）**：

| 编号 | 落点 | 修订内容 |
|---|---|---|
| C1+C2 | `README.md` | 全面重写：标题/结构表/计数对齐真实值（71 决策点 + 28 子条件、57 陷阱、14 模板、25 项自检、v0.1→v1.4）；删除不存在的 `codex skill add`，改为 `git clone` 到 `$CODEX_HOME/skills/<name>` 并注明『Codex CLI 无 skill 子命令』；补 ChatGPT（Custom GPT 知识文件）安装方式；用法块改为第 0 / 0.5 / 1–7 步 |
| C3 | `templates/segmentation_eval.py` | docstring 中 PA 由被证伪的 0.6566 改为正确值 **0.9899**，同时保留『原公式口径写错曾得 0.6566』的说明（模板自身即 T52 的示范）；第 ③ 条补精确数字（112 张中 6 张 <0.4、最差 2 张 0.000）；版本号 → v1.4 |
| C4 | 四个文件 | 版本号统一 v1.4：`SKILL.md`（标题 + frontmatter metadata.version）、`陷阱库.md`、`论文报告结构模板.md`（v0.1 → v1.4，注明内容基线 = 原 v0.3、并入统一编号）、`templates/README.md` |
| C5 | `SKILL.md` | 第四节标题『6 步』→『7 步正向推导 + 第 0.5 步数据形态直读』；顶部『使用』行同步 |
| C6 | `SKILL.md` | 分类器『代表题』补齐新决策层索引（H01→STEP B′、H04→STEP R、F03/2025C→STEP E/G、H02/H03/F01/F02/F04→预测类）；『判类要点』新增**三类易漏归属任务形态**路由条目（分割/像素级、栅格场图到图预报、复指数叠加） |
| C7 | `SKILL.md`、`陷阱库.md` | 移除正文中的本机路径（3 处），改为指向本 CHANGELOG 的『实测数据存档』 |
| C8 | `陷阱库.md` | 笔误修正：『重测实证层』→『实测实证层』（全库 4 处） |
| C9 | `论文报告结构模板.md` | 删除与实况不符的『24 项』计数（改为『既有自检清单』），并在 v1.4 说明行给出当前准确数 **25 项** |
| C10 | `templates/README.md` | 依赖说明重写为三档：顶层依赖（numpy；两个分割模板另需 Pillow）/ 局部依赖（scipy、scikit-image、cv2、scikit-learn、xgboost、statsmodels、imblearn、matplotlib）/ 已兜底（全仓仅 4 处真实 try/except，其余缺失会直接 ImportError） |
| C11 | `templates/polygon_to_mask.py` | 占比字段拆为双口径并在 docstring 写明不可混用：`coverage_pct_*`（逐图：先算每图占比再取统计量）与 **`positive_pixel_ratio_pct_global`**（全局：Σ正样本像素 / Σ总像素）；`main()` 口径对比同时报两种差值 |
| C12 | `templates/segmentation_eval.py` | 最差样本循环改为复用一份 `{stem: (pred, gt)}` 配对映射，消除原先每图重新 glob 全目录的 O(k·n) |
| C13 | `SKILL.md` | 第四节新增**阶段时间预算表**（缺省 72 h 赛程：读题/定类 ~10%、假设与建模 ~25%、求解与验证 ~35%、自评与写作 ~30%），附『超预算 50% 先过工程闸门』的预算纪律 |
| C14 | `tests/smoke_test.py` | **新增**：合成图像 + YOLO 归一化多边形（含 1 张空标签图）跑通 `polygon_to_mask.py`；人为制造偏移（FP/FN）与全空预测跑通 `segmentation_eval.py`，断言双口径字段、最差样本清单与三联图落盘；对全部 14 个模板做 AST 语法检查。实测结果：**全绿（退出码 0）**，最差样本 Dice 0.0 被正确识别 |
| C15 | `SKILL.md` | frontmatter：description 扩展触发词（增补『中国研究生数学建模竞赛/研赛』『混合类』『跑通数据到结果』『成文论文报告』）；新增 `metadata.version = 1.4` 与 `metadata.updated` |
| C16 | 本文件 | v1.4 条目（即本条） |

**验证记录**：`tests/smoke_test.py` 退出码 0（AST 14/14 + 两分割模板端到端全 PASS）；计数自查 = 陷阱 **57 条**（T1–T57 连续无缺口）/ 模板 **14 个** / 自检清单 **25 项** / 编号决策点 **71 个 + 28 条子条件**；全仓 grep 确认无本机路径、无『重测实证层』、无 v1.0 残留串；各文件首行版本号均为 v1.4。

**保留内容声明**：分类器、全部 71 个编号决策点与 28 条子条件、57 条陷阱、14 个模板的算法实现、论文模板章节与 25 项自检清单——**方法论内容一字未改**。v1.4 不含任何新增结论（与 v1.3 的 [G] 实证层性质不同）。

**下一步**：2025C 问题 2/3/4 实做（见交接文档第七节）；可选工程化项见修改清单 D1（SKILL.md 拆 references/）、D2（选许可证）、D3（加 .gitignore）、D4（其余模板补 CLI/测试）。

---

## v1.5 —— 2026-09-10（多目标检测与定量测量层 [G]）

**动机**：用 Skill 连续实做同一真实赛题的后续小问——**正弦状目标的自动聚类与参数表征**（问题 2）、**复杂目标的轮廓线提取与粗糙度指标计算**（问题 3）——暴露 5 处**可迁移**的方法缺口。

**泛化声明（重要）**：本版**刻意不过拟合到具体赛题**——不写入任何题面常数（孔径/周长/像素标定值）、不写文献经验公式的具体系数、不写"某图有几个目标"的结果数字；只保留**判据、机制与实测量级**，使规则可直接迁移到同类图像任务（多目标检测、参数化搜索、定量测量）。

**吸收清单**（5 条决策点 + 6 条陷阱 + 1 个模板，全部标 [G]）：

| 类型 | 编号 | 内容（可迁移判据） |
|---|---|---|
| 决策点 | **B6** | **形态－形貌分离**：目标是局部起伏/表面形貌而观测含由几何关系或姿态决定的宏观成分时，先剥离可参数化的趋势项、对残差定量（实测未去趋势时指标约为去趋势后 2 倍）；反例边界=目标本身是整体形态时不得去趋势 |
| 决策点 | **C5** | **参数化搜索的去重判据定义在观测空间**（容差带重合比例/区域 IoU/内点率），而非参数空间欧氏距离；两种空间并用取合取（实测同目标的近似解在参数空间分散达尺度的一大截，参数阈值与聚类均不收敛） |
| 决策点 | **C6** | **多目标枚举与归并双机制**：枚举靠"检出即抑制"、归并靠聚类判据，功能正交不可互替（实测关掉抑制改成事后聚类，25 个候选退化为 1 条、其余目标从未进入搜索视野） |
| 决策点 | **D4** | **尺度相关指标必须连同尺度声明**：给出采样点数/等效间距 + 滤波尺度，追加尺度曲线与幂律/分形指数（实测指标随采样密度单调变化、约 120 点后收敛） |
| 决策点 | **D5** | **有效分辨率构成指标物理下限**：先做像素↔物理量标定，再检查待测量是否显著大于分辨率；小于分辨率的细节只能声明不可分辨（实测去噪尺度的影响远大于采样方式选择——前者逾 50%，后者 <1%） |
| 陷阱 | **T58–T61** | 参数空间去重替代观测空间重叠；贪心搜索不抑制邻域；尺度相关指标不声明尺度；把非目标成分计入形貌类指标 |
| 陷阱 | **T62–T63** | **工程实现类**：交付物数字手写导致与产物不一致（改数据后漏改）；Windows 中文路径下 `cv2.imwrite` **静默失败**致批量产物丢失而日志仍报"已保存" |
| 模板 | `multi_target_dedup.py` | 多目标去重与聚类骨架：观测空间重叠 + 参数距离合取判据 → 连通分量聚类（C5/C6 的可运行骨架） |

**实测数据存档**：产物 `G:\AgentZone\huawei2025c`（`q23_sine_detect.py` 参数化检测器 + `q2_cluster.py` 聚类 + `q3_jrc.py` 轮廓与尺度指标 + `make_analysis_figs.py`；结果表、可视化与采样研究 JSON 见 `work/`）；报告 `C:\Users\18107\Desktop\Hermes Work\2025C_问题2与问题3_实现报告.docx`（9 表 6 图，三重校验 ok）。

**保留内容声明**：v1.4 全部内容未动——分类器、71 个编号决策点与 28 条子条件、57 条陷阱、14 个模板、论文模板章节与 25 项自检清单，原样保留；本版为纯增补（新增 5 决策点 + 6 陷阱 + 1 模板，编号顺延不重排既有编号）。

**对 2026 年新题的准备度说明**：v1.5 补的三类能力——**多目标枚举**、**参数化搜索去重**、**定量测量的尺度纪律**——是图像类赛题（检测/计数/测量/形貌表征）的高频骨架，与具体题目无关；使用时应仍按第二节分类器定类、按第三节决策树取法，**禁止把本版任何"实测数字"当作新题的结果**（实测数字仅用于判断"量级是否合理"，不构成任何题目的答案）。

---

## v1.5.1 —— 2026-09-13（算力与交付形态层 [U]）

**性质**：**不含任何方法论结论**，不参与 D/H/F 溯源，也不得据此修改任何决策点——记录的是**工作条件**。

**动机**：用户约定「数学建模可随意使用深度学习；我有 AutoDL 账号，你只需把所有需要的数据与程序准备好，我在算力平台算完把结果给你」。这解除了此前压在所有 DL 方案上的**本机 6GB 显存天花板**，同时新增一种交付物形态（可上传即跑的任务包）与一条新接口（远端执行 → 人工回传结果）。

**新增内容（第六节）**：

1. **6.1 工作条件**：DL 可随意用；算力 = 用户 AutoDL；三方分工（助手备数据+程序 → 用户执行 → 回传 → 助手出数出图）。三条纪律：先按最优方案选型再看资源 / 同时给「本地能否跑」的判断 / 远端无过程证据故任务包必须自带日志与自检。
2. **6.2 任务包规范**：`run.sh`（幂等入口 + 环境自检）/ `requirements.txt`（一律 pin `==`）/ 主程序路径全参数化（`--data-root`/`--out-dir`/`--seed`）/ `README.md`（3 步跑通 + 预期耗时 + 峰值显存 + 产物清单）/ 大数据集不进包 / `results/`。硬约束：禁写死本机路径、必须落 `run.log`、固定并回写 seed、支持 checkpoint 续跑。
3. **6.3 回传契约四件套**：`results.json`（机器可读，喂 registry）+ `run.log`（远端唯一过程证据：命令/环境/耗时/峰值显存/报错原文）+ 图与表产物 + `env.txt`（torch/CUDA/GPU + nvidia-smi）。**回传前禁止手工改数字**（T62 教训）。
4. **6.4 AutoDL 实操要点（官方文档核对）**：数据盘 `/root/autodl-tmp` 50GB 起可扩容、关机不丢失、不能保存进镜像；大文件走公网网盘且从网盘下载**固定落到 autodl-tmp**；SCP/Rsync 用**映射端口而非 22**；`source /etc/network_turbo` 学术加速、**用完必须 `unset http_proxy https_proxy`**；`HF_HOME` 指向 autodl-tmp。**铁律 = 一切大文件指向 autodl-tmp（系统盘小）**。
5. **6.5 DL 不是免罚金牌**：DL 必须与基线/经典方法同协议对比、评估口径沿用 STEP E、报结果必须带训练成本、不为用 DL 而用 DL。
6. **6.6 三侧接口边界表**：助手（任务包/数值/图）/ Qwen（判据/看图，**不出任何数值**）/ 用户（执行/回传）；**唯一数值事实源 = `registry.json`**。

**保留内容声明**：第一至五节全部内容未动——分类器、71 个编号决策点与 28 条子条件、63 条陷阱（T1–T63）、15 个 .py 模板、论文模板章节与 25 项自检清单，**方法论内容一字未改**；本版为纯增补。

**版本号说明**：本条取 **1.5.1**（补丁级）而非 1.6——1.6 预留给待决的方法论增补（弱响应区不得用宽带 argmax / 不得用响应值当最小二乘权重，拟作为 T64 入陷阱库）。**方法论层与环境约定层的版本号不混用。**

**本次发现的文档缺陷（未改，待随 1.6 一并修订）**：SKILL.md 第五节『配套文件』计数已过期——正文写「陷阱库共 53 条」「templates 13 个」，实际为 **63 条（T1–T63）** 与 **15 个 .py（+README 共 16 条目）**；且该节未索引 v1.5 新增模板。性质同 v1.4 的 C1–C16 文档脱节。

---

## v1.5.2 —— 2026-09-14（三侧协作与核对层 [Q] + 清还文档债）

**性质**：**[Q] 流程层，不含任何方法论结论**——不参与 D/H/F 溯源，不得据此修改第三节任何决策点。与 [U] 同族，故按 v1.5.1 定下的「方法论层与环境约定层的版本号不混用」纪律取**补丁级**；**v1.6 仍预留给 T64 方法论增补**。

**动机**：用户提出三侧协作工作流（助手 / Qwen3.8-Max / 用户），并要求：① 离散接触 + 盲态并行；② 严格按「Qwen 建模 + 助手编程」分工，**数据权威移交 Qwen**；③ 加入反谄媚核对；④ 将机制并入 Skill。经逐项实测，Qwen 侧通道实为 **`qoderclicn`**（Qoder CN CLI，**非 `qwencn`**），走用户既有 Qoder 订阅。

**新增内容（第七节，7 小节）**：

1. **7.1 三侧分工与权威划分**：Qwen = 建模权威 + 数据（口径/常数）权威；助手 = 实现权威 + 账本权威；用户 = 终审权威。
2. **7.2 通道实测配方**：`--tools "" -p -o json -w <dir> -m "Qwen3.8-Max"`，提示词走 stdin；**六个实测坑**（`--tools ""` 变长参数须置首位否则 exit=0 零输出 / `--` 分隔符 exit=42 / 会话按项目目录存储故 resume 须同 `-w` / 长中文提示词走 stdin / `-o` 仅 text|json|stream-json / `total_credits` 疑为会话累计值标【不确定】）。
3. **7.3 七阶段 × 六接触点**：S0 入库 → S1 盲态并行 → S2 交叉比对 → S3 分歧裁决 → S4 规格冻结 → S5 实现产出 → S6 双层核对 → S7 交付；含**会话编排纪律**（盲态/看图/换视角必须新建会话）与 **T1/T2/T3 通信预算分层**。
4. **7.4 反谄媚核对协议**：盲态先做 + 强制红队模板（禁赞同语；「未发现反驳」须附尝试过的 3 个方向）+ 双向自红队。**实测有效**：同一方案得 6 条实质反驳、通篇零赞同语。
5. **7.5 分歧裁决阶梯 L1–L6**：实验 → 题面 → 决策树/陷阱库 → 文献 → 双轨保留 → decision_memo；**禁投票平均与和稀泥**。
6. **7.6 数值权威与防漂移（双账本）**：★ **解除「Qwen 不输出任何具体数值」禁令**（v1.5.1 及以前的规定），改为分两层记账——**spec 层**（常数/阈值/口径，Qwen 权威，记入 `口径表.md` 并强制标 `依据来源`=题面|领域知识|文献|推测）与 **result 层**（数据算出的结果，只能由代码产出并记入 `registry.json`；Qwen 给出的结果类数值一律降级为 `预估值`，不作文付数字）。冲突规则：spec 层以 Qwen 为准，result 层以 registry 为准。
7. **7.7 通信台账与降级**：`transcript.jsonl` 逐轮记账；六类降级/中断处置。

**风险迁移声明（重要）**：原禁令靠「不让它说数字」防漂移；解除后防漂移改由**双账本 + 依据来源标注 + 口径冻结**承担，**不是取消防护**。新增的残余风险是「spec 数值跨轮次静默变更」，由 S4 口径表冻结 + 变更记录兜底。

**清还的文档债（v1.5.1 登记的两项，本版已修）**：

1. 第五节『配套文件』计数过期 → 更正为 **陷阱库 63 条（T1–T63）**、**templates 15 个**，并补索引 v1.5 新增的 `sinusoid_hough.py` 与 `multi_target_dedup.py`（含准确描述，取自两文件自身的 docstring）。
2. 标题版本号与 frontmatter 不一致（标题停 v1.5、frontmatter 为 1.5.1）→ 一并为 v1.5.2。

**本版额外清点发现（v1.4 的 C1–C16 未覆盖，本版一并修）**：

1. **决策点计数错**：v1.4/v1.5/v1.5.1 正文均称「71 个编号决策点」，实测 **76 个**（A3 B6 C6 D5 E5 F10 G5 M13 P7 R5 S5 U3 V3）。71 疑为 v1.2 加入 STEP E（E1–E5，恰 5 条）之前的陈旧值。子条件 **28 条**复核无误。
2. **计数散落且互相矛盾**：同一事实在正文出现 5 种数字（陷阱 50/53/53/57/63）、模板 4 种（11/11/13/14/15）。已改为**只在头部维护一张『资产清点』**，并声明变更摘要不再复制计数。
3. **B5/B6 编号乱序**：v1.5 把 B6（形态-形貌分离，[G]）插入 B4 与 B5 之间，导致阅读顺序为 `B4 → B6 → B5`。已物理交换两块位置，恢复 `B4 → B5 → B6`（**标签语义不变**：B5 仍是「通道/变量选择」[D08]，B6 仍是「形态-形貌分离」[G]）。
4. **§3.1 路线图小标题漏项**：原为「预处理 → 特征 → 谱估计 → 模型 → 评估 → 时空预报」六项，实际层序为 A→B→B′→C→D→R→E→G 八项，漏 STEP E 与 STEP G。已补齐。
5. **头部变更摘要臃肿**：v1.1–v1.5.1 六段长摘要与 `CHANGELOG.md` 重复，且各自携带已失效的计数。已压缩为一张**版本演进索引表**（保留每版动机一行 + 溯源标记），完整叙事仍以本文件为准。
6. **`README.md` 整份过期（v1.4 与 v1.5.1 两次都没抓到）**：README 停在 **v1.4**，携带全套失效计数（71 决策点 / 57 陷阱 / 14 模板 / CHANGELOG 链止于 v1.4），溯源标签表缺 `[U]` 与 `[Q]`；更严重的是它的**「用法」步骤编号与 SKILL.md 第四节错位**——README 把「分类器定类」写成「第 1 步」，而主文件第 1 步是「问题重述」、分类器发生在第 3 步建模时。已整份重写并对齐主文件。**教训**：v1.5.1 的缺陷登记只扫了 `SKILL.md` 一个文件，未做全目录计数对撞——本版改为**以清点脚本对撞全部文件**。

**保留内容声明**：第一至六节**方法论内容一字未改**——分类器（第二节）、全部 **76** 个编号决策点与 **28** 条子条件、**63** 条陷阱、**15** 个模板算法、论文模板章节与 25 项自检清单，全部原样保留。本版为**纯增补 + 计数更正 + 排序修正**，**未新增、未删除、未修改任何方法论结论**。

> **勘误（第二轮复核后补记）**：本节初稿曾声称 §6.6 改写为索引段「**无信息丢失**」——该断言**可证伪**，
> 见下方『第二轮：独立审查发现与修订』第 2 条。实际丢失两项，已补回。

---

### 第二轮：独立审查发现与修订（同日）

新增第七节后，另派**独立子进程**通读全部六个文件（含与 `SKILL.md.v1.5.1.bak` 逐行对比）并复核计数。
审查确认了本版的计数更正（76 / 28 / 63 / 15 全部属实，第四节内部无缺环），同时查出下列问题，**均已修订**：

**A. 本版自身引入的缺陷（已修）**

1. **§7.5 与 §7.6 互相矛盾**（最危险）：§7.5 定「L1 跑实验**永远优先**」，§7.6 却定「spec 层冲突**以 Qwen 为准**」——
   同一分歧会有相反裁决，且两节互不引用。反例：ω 取 0.95 还是 0.85，既属 spec 参数、又完全可实证。
   **修法**：§7.6 增『裁决优先级』表（题面明给 > 可实证 > Qwen 依据来源分层 > 助手实现约束 > 双轨/memo），
   明确 **L1 适用于一切可实证分歧，spec 层也不例外**；「以 Qwen 为准」只在不可实证时生效。
2. **`依据来源` 只作标注、不作优先级** → 让「领域知识」项既可免于标注、又高于一切。
   反例正是本工作流自己实测过的：Qwen 注入 ε=0.02（Kosmhieder 惯例，标「领域知识」）vs 题面 0.05
   → 三条铁律全部合法 → MOR 系数 2.996 vs 3.912 相差约 30%，命中 T25 全盘扣分。
   **修法**：`依据来源` 升格为**裁决优先级的输入**，并加『与第三节的关系』例外条款
   （第三节已出现的常数视为题面级，Qwen 不得覆盖，只能在题面未给时补充定义）。
3. **双账本边界不可执行、且官方示例本身违规**：§7.6 铁律 3 说「同一常数两处出现即缺陷」，
   而协议 v1.1 给出的 registry 示例内联了 `"epsilon=0.05"`。**修法**：明确边界——
   `口径表.md` 存取值与定义，`registry.json` 只存执行结果且**引用口径表的键、不复制取值**；
   并给出边界例外（E1/T51 的「正样本像素占比」按 result 记账）。
4. **§6.6 改写丢失两格 + 责任悬空**：丢掉「**不替用户下单/操作账号**」与「图与**正文**」，
   导致论文正文责任人在 [Q] 层空缺（而第四节第 6 步与阶段预算表都把论文列为交付物）。
   **修法**：§7.1 表补回两格 + 新增『论文正文归属』补记。
5. **阶段编号撞名**：§7.3 的 `S0–S7` 与 §3.1 STEP B′ 的谱估计决策点 `S1–S5` **同文件同名**，
   且协议 §11 又按 `S1–S3` 引用阶段，必然串号。**修法**：协作阶段全面改名 **「阶段0–阶段7」**，
   并加『编号避碰』说明。
6. **无数值红队**：六接触点只对**方法**做红队，无人核对 spec 数值的 `依据来源`——
   `推测` 被写成 `领域知识` 无法被发现，而它直接抬升该值的裁决优先级。
   **修法**：§7.4 增设『数值红队』纪律（逐条复述依据 + 给出反例场景）。

**B. 审查确认的结构性缺口（已修）**

7. **主断点：第四节 7 步在正文里从不引用第五/六/七节**（正则扫描仅命中 1 处且是无关的假设约定）。
   `[U]`/`[Q]` 两层与主流程只靠文件头『使用』一行串联。**修法**：第四节新增
   **〔层间映射表〕**，把「步 ↔ 取法来源 ↔ 配套/控制层 ↔ 协作阶段 ↔ 出口闸门」逐行钉死。
8. **T63 在 SKILL 正文无落点**（T62 有）。**修法**：§6.2『易犯硬约束』补入
   Windows 中文路径下 OpenCV `imwrite` 静默失败条，并注明其为 T63 的交付链落点。
9. **「变更记录」悬空**：§7.6 要求冻结后改动走变更记录，却无文件落点、无签发人。
   **修法**：定为 `04_spec/变更记录.md`，并给字段清单（含签发人）与目录结构位置。
10. **陷阱编号账目对不上**：原『历次追加』漏记 **v1.3 整批（T54–T57）**，账面 48+2+3+6=59≠63。
    **修法**：改为完整七批账目（30+12+6+2+3+4+6=63），并补层列表漏列的
    「无标注实战层 / 多目标检测与定量测量层 / 工程实现类」。

**C. 复发级文档债（第三次出现，已清）**

11. `论文报告结构模板.md:132`「陷阱库 53 条」、协议 `:97`「57 条」、`README.md`（v1.4 / 71 / 57 / 14）
    三处过期计数。v1.4 的 C1 修过一次、v1.5.2 首轮只修了 SKILL 自己那份。
    **修法**：三处全部改并注明来源；**计数现只在两处维护**（SKILL 头部『资产清点』+ README 表）。
12. **协议与 SKILL 直接矛盾**：SKILL 已解除「Qwen 不输出数值」禁令，协议 v1.1 仍在执行该禁令。
    **修法**：协议**降级为纯操作手册 v1.2**，规则权威单点化到 `SKILL.md §7`；**v1.1 已删除**
    （两份文件持有冲突规则，保留即事故源）。

**D. 审查提出但本版未改（留作后续，非缺陷）**

- **H04 双路由悬空**：分类器把 H04 归图像视觉类（STEP R），而 [H04] 派生的机理双轨决策点住在 3.3 混合类，
  按分类器走永远进不了 3.3 第二块。
- **「优化」动词无树可进**：分类器动词表含「优化」，3.2 无优化层，优化取法实际落在第四节第 4 步
  （LP/QP/GA/PSO/DEA-BCC/克里金）——方法层内容长在流程节里。
- **「按小问分归两类」（D08）与「混合类」（D09）界分条件未定义**。
- **实测量级数字跨文件手写**：`0.7377` 共 **18 处**散在 6 个文件、`2.996` 共 **10 处**散在 6 个文件、
  `0.5844` 5 处 / `3.912` 5 处 / `0.3863` 4 处 —— 违反自家 T62 与 §7.6 铁律 3
  （「同一常数在两处出现即缺陷」）；本版的『计数纪律』只管计数，不管实测量级。
  > **对该条的自我更正**：审查报告称「**已被证伪的 `0.6566` 仍有 6 处**」，经逐处核对**不成立**——
  > 全库 `0.6566` 实为 8 处（`templates/segmentation_eval.py` 1 / 本文件 4 / `SKILL.md` 1 /
  > `论文报告结构模板.md` 1 / `陷阱库.md` 1），**每一处都是刻意保留的警示性引用**
  > （形如「原 PA 公式分母口径写错曾得 0.6566，修正后 0.9899」），用途正是记录该错误本身，
  > **不是残留的失效数值**，v1.4 的 C 系列修复是完整的。此条属审查方的上下文误判，
  > 记录在此以免后续再被当作缺陷处理。
- **其余重复项**：溯源编号表写 4 遍（后 3 份已过期）、三盲评阈值双份、decision_memo 近逐字重复、
  「三层自检」同名两义、图表证据角色表两套 schema（五列 vs 八列）、§6.5 自认是既有规则的复用。

> 以上 D 类属**结构优化**而非错误，改动面大（涉及第三节与第四节的职责重划），
> 未在 v1.5.2 内实施，留待 v1.6 或专门的『一致性修订版』一并处理。

### 第三轮：模型纪律修正（用户裁定，同日）

**用户裁定**：Qwen 侧**只调用 `Qwen3.8-Max`**；本任务中 **Qwen 负责建模，助手负责编程及其余细碎事项**；
**「低成本档」的任务由助手本人承担，不下放给任何更小的模型**；**删除全部 `Qwen3.8-Flash` 相关内容**。

**背景：这是一处由助手引入的设计错误，已作废。**

- **错在哪**：助手在 §7.3 起草「通信预算分层」时，**凭「Flash = 更快更便宜」的命名直觉**
  自造了一个 T2 档，把「看图 / 公式复核 / 失败案例归因」划给 `Qwen3.8-Flash`。
  两条理由都不成立：①「Flash 更便宜」**从未实测验证**（`total_credits` 语义当时仍属【不确定】）；
  ② 这三项**恰恰是最吃判断力的环节**，其中「看图」更是 skill 6.6 明确指定给 Qwen 侧的核心能力。
  实质是**把高价值任务错放进廉价模型桶**，与本工作流「最大化 2.4T 模型建模能力」的初衷相反。
- **正确模型**：只有两层——**「该 Qwen 建模」与「该助手自己干」**。
  助手做的是代码实现、调试、数值计算、画图制表、文件组织、格式整理、数据搬运等**工程杂事**；
  凡涉建模判断（判据、口径、方案取舍、看图、公式复核、失败归因、红队、创新点）一律交 `Qwen3.8-Max`。
  **不占 Qwen 轮次**是助手的职责，不是换个模型省下来的。

**落点修订**：

| 文件 | 改动 |
|---|---|
| `SKILL.md` §7.3 | 「通信预算分层」重写：删去 T1/T2/T3 三档与全部 Flash 提及，改为**两层**——「交给 Qwen3.8-Max（建模判断）」与「助手自己做（编程与细碎事项，不占 Qwen 轮次）」；补『模型纪律』『分工定性』两段；并明确**助手不得自行签发建模结论**（产出结论须经 Qwen 复核） |
| `SKILL.md` §7.7 | 降级表「credits 逼近额度上限」行：由「降级到 `Qwen3.8-Flash`」改为**「不降级模型」**——改为压缩接触点（合并 ③④、砍 ⑥、优先砍 T2 类复核）并发 `decision_memo` |
| `三侧协作工作流协议_v1.2.md` §2 | 模型行改为**「本工作流唯一使用的模型」**；删去「可用模型清单」整段（含 Flash）；补分工定性一行 |
| `qwen-call.ps1` | `-Model` 参数注释由模型枚举改为**「固定 Qwen3.8-Max」**，并注明保留该参数**仅为通道排障**、协作流程不得据此分档 |

**验收**：全库（5 个技能文件 + 协议 + 适配器 + 流程图 SVG）**`Flash` 零命中**，已脚本核验。

> **教训（值得留档）**：这是一次典型的**「凭命名习惯代替实测」**——「Flash/Pro/Max」是产品线的
> 营销分层，不等于本任务的价值分层。凡是「哪个模型干哪档活」这类判断，若不实测成本与质量，
> 就不该写进流程。本层最终采用的分层依据是**任务性质（建模判断 vs 工程杂事）**，不是模型档位。

### 第四轮：分类器流程断点修复（同日）

修掉第二轮审查中「分类器判了类却无路可走 / 走了会漏」的三处**流程断点**。**均为纯补充，未新增或修改任何决策点**（计数仍为 76）：

1. **「按小问分归两类」（D08）与「混合类」（D09）界分未定义**——原文只说前者按小问分别归类、
   后者是两段式，但没给**判据**，实际执行时无从下手。**补判据**：看**耦合强度**——
   *后问用不用前问的结果？用 → 混合类（存在显式跨问数据流）；不用 → 分归两类。*
2. **H04 双路由悬空**——分类器把 H04 归「图像视觉类·STEP R」，而 [H04] 派生的
   「显式物理反演 vs 物理衍生场 + 网络内嵌」双轨决策点却住在 **3.3 混合类**，
   **按分类器单向走永远进不了 3.3 第二块**。**补入口**：栅格场预报题若同时给出领域机理公式，
   必须**再进 3.3 混合类第二块**取双轨对照。
3. **「优化」动词无树可进**——分类器任务动词列含「优化」，但 3.2 无优化层；
   优化取法实际散落在**第四节第 4 步「求解」**（LP/QP/GA/PSO/DE/贝叶斯、反向优化、组合降元、
   DEA-BCC、克里金），**方法层内容长在流程节里**。**补落点说明**：定类仍按数据形态判，
   但取法跳到第四节第 4 步，避免在三节里空找。
   > 审查方建议的根治方案是把优化方法**下沉为 3.2 的新 STEP**（计数将 76 → 约 82，需同步三处计数）。
   > 本次**未采纳**——该重构涉及第三节与第四节的职责重划，改动面大，留待专门的一致性修订版；
   > 本轮的落点说明已消除「无从下手」的实际障碍。

**验收**：三处修复均经脚本核验（`后问用不用前问的结果` / `修 H04 双路由悬空` / `消「无树可进」` 全部命中），
决策点计数仍为 **76**，全库 `Flash` 仍为 **0** 命中。

### 第五轮：外部结构审查（Qoder 侧）发现与修订

**来源**：`C:\Users\18107\Documents\Qoder\huawei-cup-skill-结构分析与优化建议.md`（2026-09-14）。
该报告核验资产账目属实（76 / 63 / 15 与陷阱编号连续性、冒烟测试可跑通），
并指出 **4 处文档-内容脱节**。**四处经逐条复核，全部成立，无一处误报**（与前几轮不同）。

**P0 —— 三处实修**

1. **T63 落点自相矛盾（最讽刺的一处）**：`陷阱库.md:408` 与 `templates/README.md:31`
   **均声称 `sinusoid_hough.py` 内含 `imwrite_u`（Unicode 路径安全保存）**，
   而该模板实际用的是**裸 `cv2.imwrite`**（原 L144 / L147）——**模板自己踩中自家 T63**。
   **修法**：补入 `imwrite_u(path, img)`（`cv2.imencode(...)[1].tofile(path)` +
   **落盘后校验文件存在且非空**），两处调用全部替换。
   **实测证据**（本次现场复现，非推断）：中文路径下裸 `cv2.imwrite` **返回 `False`、不抛异常、文件确实不存在**；
   `imwrite_u` 成功落盘（146 B）；路径非法时按设计主动抛错。T63 描述的现象与修复效果均已坐实。
2. **测试文件残留本机路径**：`tests/smoke_test.py:13` 的
   `PYTHONPATH= "<用户目录>/anaconda3/envs/pytorch/python.exe"`（原为一条真实本机绝对路径，
   此处已脱敏）—— v1.4 的 C7（清除正文本机路径）当时只扫了 `.md`，**`.py` 漏网**；
   且 `PYTHONPATH` 应指模块目录而非解释器，写法本身不成立。
   **修法**：改为 `python tests/smoke_test.py`。
3. **测试断言阈值过期**：`smoke_test.py:69` 断言 `模板数量 >= 14`，实际已 15 个。
   **修法**：该处**不再硬编码任何数字**，改为非数字的健全性检查，
   精确计数交由第 5 步的清点脚本单源对账——**根治「同一个数字写在两处」这一结构性病因**，
   而不仅是把 14 改成 15。

**P1 —— 把计数纪律机械化（报告评为收益最大的单项优化，已采纳）**

4. **「清点脚本」此前不存在**：SKILL.md 头部写着「以清点脚本输出为唯一事实源」，
   但仓库内并无该脚本——**声明是愿景不是机制**，计数对账仍靠人肉，这正是文档-计数脱节
   在 v1.4 / v1.5 / v1.5.2 三次失效的**共同根源**。
   **修法**：新增 **`tests/audit_assets.py`**（**15 项**检查：陷阱编号连续性与起点 / SKILL 头部
   4 项声明 vs 实测 / README 表 4 项声明 vs 实测 / SKILL↔README 4 项交叉相等 / 无未登记前缀），
   并接入 `tests/smoke_test.py` 成为第 5 步。**不符即非零退出码，可直接挂 CI。**
   同时更新 SKILL.md 头部『计数纪律』与 README 表，把「清点脚本」明确指名为该文件。

**验收（全部实际执行）**：

| 项 | 命令 | 结果 |
|---|---|---|
| 清点对账 | `python tests/audit_assets.py` | **15/15 PASS，退出码 0** |
| 冒烟测试（含第 5 步） | `python tests/smoke_test.py` | **全部通过，退出码 0** |
| T63 复现与修复 | AST 提取模板内 `imwrite_u` 实测中文路径 | 裸 `imwrite` 返回 False 且无文件；`imwrite_u` 成功落盘 |

> **顺带修掉的脚本自身缺陷**：首版 `audit_assets.py` 报出「README 缺子条件声明」，
> 复核后确认是**脚本正则写错**（README 的 `**` 包住整句 `76 个编号决策点 + 28 条子条件`，
> 而正则要求 `28` 自带 `**`），非文档问题——已修正则。
> 另修 stdout 编码（Windows 代码页下中文乱码，致人读输出不可用）。
> **教训与第四轮同源：工具报出的问题也要复核，不能照单改文档。**

**未采纳项（报告 P2/P3，留作后续）**：

- **P2 SKILL.md 瘦身**：拆 `references/`（第七节三侧协作、6.4 AutoDL 实操表移出正文）。
  收益明确（主文件已 100 KB / 715 行，每次全量进上下文），但**与『规则单点权威』存在张力**——
  拆分会把权威分散到多文件，需先定「谁是真源、谁是指针」的边界，故本轮不动。
- **P3 重复项单点化**（三盲评阈值双份、decision_memo 5 处、「三层自检」同名两义、溯源表 3 遍）
  与杂项（LICENSE/.gitignore、T64 补入或撤销预留、更多模板端到端覆盖）——同前几轮，留待一致性修订版。

**待决（移交后续）**：

1. `total_credits` 是否为会话累计值——【不确定】，需多会话对照核实。
2. Qoder 订阅的额度上限与计费口径未知，影响 7.7 的额度降级阈值（**注**：该处已改为「不降级模型」，
   故额度压力转为压缩接触点，此项优先级下降）。
3. ~~原定 v1.6 的 T64 方法论增补仍未入陷阱库~~ → **v1.6 已兑现**，见下。

---

## v1.6 —— 2026-09-14（响应提取与加权拟合层：T64 [G]）

**性质**：**方法论层增补**——即 v1.5.1 预留的那个版本号，兑现于本版。参与 [G] 溯源。

**动机与过程**：v1.5.1 把 v1.6 预留给「弱响应区不得用宽带 argmax ／ 不得用响应值当最小二乘权重」，
但当时**只有结论、没有证据**，摘要仅一行。此后多轮均因「无出处」而拒绝写入（skill 的溯源纪律：
无出处或『待核实』的断言一律不写入）。**本版找到 2025C 问题2 的实测原始记录后补齐证据链，正式落为 T64。**

**证据来源（全部为可追溯的原始产物，非据摘要转写）**：

| 材料 | 位置 | 作用 |
|---|---|---|
| `q2_fit_quality.py` | `G:\AgentZone\huawei2025c\` | **案发现场**：`obs_points()` 逐列 `np.argmax`（带内取行）；`least_squares(lambda q: w*(sine(q,x,P)-y))` 响应值加权 |
| `work/q2_fitq/fit_quality.md` | 同上 | 4 口径对照表（窄/宽带 × Hough 参数/最小二乘重拟合） |
| `work/q2_fitq/fit_quality.json` | 同上 | 逐条数据 66 条 |

**本次分析（在原始数据上重新统计，非引用他人结论）**：

1. **带宽效应**：±10px → ±25px，R² 均值 **0.9563 → 0.8567**（降 0.0996），
   R²>0.95 占比 77.3% → 48.5%，MSE<25mm² 占比 100% → 39.4%；**66/66 条全部变差，无一例外**。
2. **剂量-反应（关键，坐实了「弱响应区」这一限定词）**：按检测得分分箱——
   `[0,0.15)` 组降 **0.1049**、`[0.15,0.25)` 组降 0.0797、`[0.35,1.0)` 组仅降 **0.0176**。
   **弱响应区危害约为强响应区的 6 倍**，单调衰减。机理：宽窗 argmax 取到的是**噪声极大值**，
   E[max of N 噪声] 随窗宽 N 增长；响应强时真峰压倒噪声，响应弱时噪声接管。
3. **响应值当权重**：窄带下 57.6% 条目更差（均值 −0.0168）、宽带下 56.1% 更差（−0.0325）
   ——**两个带宽口径下一致变差**。定性为**稳健的轻度负面效应**（不夸大为全局崩塌）。
   机理：权重本应 ∝ 1/σ²，而 `w` 是 argmax **选出来**的最大值，等于给噪声最大的点最高权重。

**新增内容**：

- **`陷阱库.md` 新增 T64**（响应提取与加权拟合类，[G]），63 → **64 条**，分 8 批。
- **`SKILL.md` §3.1 STEP G 新增〔v1.6 注·容差带打分的正确用法〕** —— 这是本版最需要辨析的一处：
  该层 G2/G3 与模板 `sinusoid_hough.py` **本就推荐容差带内打分**，而 T64 说宽带 argmax 有害，
  表面看是冲突。**实际不冲突，必须区分两种用法**：
  - **检测/打分用**（带内取最大响应作**候选得分**）→ ✅ 稳健，**保留**；
  - **提取/拟合用**（带内 argmax 的**行位置**当"观测点"，再据以重拟合或算 R²/MSE）→ ❌ **T64 陷阱**。
  正文已加入对照表钉死该边界，避免 T64 被误读为「推翻容差带打分」。

**附带教训（写入 T64）**：该脚本作者当时的注释写「窄带偏乐观、宽带更公平」，**与实测结果相反**。
实测显示**两个带宽都不安全**（窄带因贴着拟合曲线取样而自证偏高、宽带因噪声极大值而系统偏低），
且二者差异本身即识别噪声机制的判据。**「哪个口径更公平」不能靠直觉判定，必须让两种口径都跑出来对照。**

**保留内容声明**：第一至七节**方法论内容未改一字**——分类器、全部 **76** 个编号决策点与 **28** 条子条件、
既有 **63** 条陷阱、**15** 个模板算法、论文模板章节与 25 项自检清单，全部原样保留。
本版为**纯增补 + 计数同步**（陷阱 63 → 64）。

**验收**：`tests/audit_assets.py` 自动对账通过（陷阱连续性 T1–T64 无缺口；SKILL 头部、README 表、
论文模板自检清单三处计数与实测值相等）；`tests/smoke_test.py` 全绿。

**待决（移交后续）**：

1. `total_credits` 是否为会话累计值——【不确定】，需多会话对照核实。
2. Qoder 订阅的额度上限与计费口径未知（优先级已降，见 v1.5.2 说明）。
3. Qoder 侧结构审查报告的 **P2（SKILL.md 拆 `references/`）与 P3（重复项单点化、LICENSE/.gitignore）**
   未实施——P2 与「规则单点权威」存在张力，需先定边界。
