---
name: huawei-cup-math-modeling
description: 遇到华为杯/中国研究生数学建模竞赛（研赛）/数学建模竞赛的图像视觉类、数据预测类或两者混合的赛题，需要从题目出发正向推导解题方案、跑通数据到结果、并成文论文报告时使用——先分类定方法论，再按决策树逐点选方法，全程规避已证陷阱。
metadata:
  version: "1.6"
  updated: "2026-09-14"
---

# 华为杯解题 Skill v1.6（方法内核 + 工程外壳 + 三层实测实证 + 三侧协作核对层 + 响应提取纪律：图像视觉类 + 数据预测类）

> 由 2017–2021 年华为杯 11 道赛题（D01–D11）的『盲做→对照→差距分析』机制蒸馏，并经 2022A/2023D/2023E/2023F 四题盲测验证升级为盲测通过版：四题逐小问对齐度 87.5% / 81.25% / 77.8% / 87.5%，全部通过 ≥70% 闸门；再经 2024C/2024D/2025C/2025D 四题实战评估（联网比对获奖/权威方案），按评估报告『Skill 修订建议清单』合入修订，升级为 v1.0 终版：四题评分 F01=82/90/88/85、F02=84/87/86/86、F03 总评≈0.80、F04 总评≈0.84。
> **版本演进索引**（完整叙事见同目录 `CHANGELOG.md`，本节只留索引与当前计数）
>
> | 版本 | 日期 | 新增层 | 溯源 | 触发证据 |
> |---|---|---|---|---|
> | v0.1–v0.9 | 09-06 | 方法内核（蒸馏 → 盲测通过版） | D/H | 2017–2021 十一题蒸馏；2022A/2023D/2023E/2023F 四题盲测，逐小问对齐度 87.5% / 81.25% / 77.8% / 87.5%，全部过 ≥70% 闸门 |
> | v1.0 | 09-06 | 方法内核终版（合入实战评估） | D/H/F | 2024C/2024D/2025C/2025D 四题实战评估：F01=82/90/88/85、F02=84/87/86/86、F03≈0.80、F04≈0.84 |
> | v1.1 | 09-06 | **工程外壳**：MECH/INN 台账、工程闸门、诚实性机制化、三盲评协议、continuation state | [M][C] | 2025C 同题对比实验——解题方法层与 mathodology 高度同源、质量相当，差异集中在流程层 |
> | v1.2 | 09-10 | STEP E 分割/像素级专层（E1–E5）+ 2 模板 | [E] | crack-seg 4029 张裂缝图真实数据全流程实跑：轻量 U-Net 23.1 min 得 test Dice 0.7377 / IoU 0.5844 |
> | v1.3 | 09-10 | STEP G 无标注/弱标注实战层（G1–G5）+ 1 模板 | [G] | 2025C 官方附件十张钻孔展开图（244×1350，**无标注**）实做问题 1 |
> | v1.4 | 09-10 | **一致性修订**（C1–C16 文档-内容对齐）+ `tests/smoke_test.py` + 阶段时间预算表 | — | 外部复核（Codex/ChatGPT 侧）发现文档与内容脱节 |
> | v1.5 | 09-10 | B6 形态-形貌分离、C5 去重判据、C6 枚举/归并分工、D4 尺度声明、D5 有效分辨率下限 + 6 条陷阱 | [G] | 同题后续小问：正弦状目标自动聚类表征、轮廓提取与粗糙度指标 |
> | v1.5.1 | 09-13 | 第六节 算力与交付形态（任务包规范 / 回传契约四件套 / AutoDL 实操） | [U] | 用户约定：DL 可随意使用、算力走本人 AutoDL 账号 |
> | v1.5.2 | 09-14 | **第七节 三侧协作与核对层**（[Q]）+ **数据权威移交 Qwen 侧** + 全量资产清点修订 | [Q] | Qoder CN CLI 通道逐项实测（会话续跑/传图/结构化回执）+ 资产计数对撞 |
> | **v1.6** | 09-14 | **T64 响应提取与加权拟合类陷阱** + 3.1 STEP G 容差带用法辨析 | [G] | 2025C 问题2 实测原始记录（66 条裂隙带宽对照 + 响应得分分箱） |
>
> **计数纪律（v1.5.2 起）**：历史上每个版本的『保留声明』各带一套计数且互相矛盾（陷阱 48/50/53/57/63、模板 11/11/13/14/15、决策点 56/71）。自本版起**不再在变更摘要里复制计数**，只维护下面这张『资产清点』，并以清点脚本输出为唯一事实源。
>
> **清点脚本 = `tests/audit_assets.py`**（v1.5.2 补）。此前本句只是**愿景**——脚本并不存在，
> 计数对账仍靠人肉，这正是文档-计数脱节三次失效的共同根源。该脚本现已落地并接入
> `tests/smoke_test.py` 第 5 步：对账决策点 / 子条件 / 陷阱编号连续性 / 模板数，
> 并交叉校验本段与 `README.md` 表格的数字是否相等，**不符即非零退出码**。
>
> **资产清点（v1.6 实测复核，由 `tests/audit_assets.py` 对账）**：编号决策点 **76** 个（A3 B6 C6 D5 E5 F10 G5 M13 P7 R5 S5 U3 V3）· 子条件 **28** 条 · 陷阱 **64** 条（T1–T64 连续）· 代码模板 **15** 个 · 论文模板自检清单 **25** 项 · 自检脚本 **2** 个。
> **版本号预留已兑现（v1.6）**：v1.5.1 曾把 **v1.6 预留给待决的方法论层增补**（「弱响应区不得用宽带 argmax ／ 不得用响应值当最小二乘权重」），并要求「方法论层与环境约定层的版本号不混用」。**本版即为兑现**：以 2025C 问题2 实测原始记录为证据，将该增补落为陷阱 **T64**（详见 `陷阱库.md`）。
> **溯源纪律**：每条规则标注出处——D01=2017D 前景提取、D02=2018D 潮汐、D03=2019B 星图、D04=2019C 视觉情报、D05=2019D 工况、D06=2019E 变暖、D07=2020B 辛烷值、D08=2020C 脑信号、D09=2020E 能见度（混合）、D10=2021B 空气质量、D11=2021D 抗乳腺癌；H01=2022A 移动场景超分辨定位、H02=2023D 区域双碳目标与路径规划、H03=2023E 出血性脑卒中临床智能诊疗、H04=2023F 强对流降水临近预报（H 系列来自四份盲测解答第七章『试用反馈』与四份比对报告，v0.9 新增）；F01=2024C 磁芯损耗、F02=2024D 地理综合、F03=2025C 围岩裂隙、F04=2025D 低空湍流（F 系列来自四份实战评估报告『Skill 修订建议清单』与『偏离点细目』，v1.0 新增）；[M]=吸收自 mathodology 机制（v1.1）、[C]=2025C 同题对比实验实证（v1.1）、[E]=真实数据实跑实证（v1.2：crack-seg 4029 张裂缝分割 A 方案）、[G]=官方附件真实数据实战实证（v1.3：2025C 附件 1 十张钻孔展开图，无标注裂隙像素识别）、[Q]=三侧协作核对层（v1.5.2：Qoder CN CLI 通道逐项实测——会话续跑/传图/结构化回执；**与 [U] 同属流程层，不参与 D/H/F 溯源，且不得据此修改第三节任何决策点**）——两组实测的脚本与产物路径见 `CHANGELOG.md` v1.2/v1.3『实测数据存档』（正文不硬编码本机路径）。无出处或原料中『待核实』的断言一律不写入。
> **使用**：第二节定类 → 第三节决策树取法 → 第四节 7 步执行（含第 0.5 步数据形态直读）→ 第五节配套文件 → 第六节算力与交付形态（任务包/回传契约）→ **第七节三侧协作与核对层（[Q]：盲态并行 / 强制红队 / 分歧裁决阶梯 / 数值事实源）**。

---

## 一、适用范围

**触发条件（满足其一即启用）**：
- 华为杯/研究生数学建模题，提供数据（或数据需自行收集 [D06]），要求从题面出发正向推导完整方案；
- 任务形态为图像视觉类（从图像/视频/信号中提取、识别、匹配、测量、估计）或数据预测类（基于表格/时序数据回归、分类、预测、筛选、优化、评估），或两者混合（D09 双段任务）；
- 要求交付论文/报告（问题重述、假设、建模、求解、验证、评价）。

**不适用边界（明确不启用）**：
- 纯运筹/纯组合优化题（仅调度、路径、排程、分配，无数据建模环节）：模型决策树不适用，仅第四节顶层流程可作通用脚手架；
- 纯物理/公式推导题（直接推导解析公式，无数据与模型选择环节）；纯定性/纯文档分析题。

**置信度说明**：『题面数字即官方参数』『参考文献即泄题信号』『对比实验是得分点』为 11 题反复印证的强规律；原料中『待核实/中可信度』的单项细节（D03 角距截断、D02 多项式次数、D07 的 99.6% 准确率）只作量级参考，不作规则依据。盲测四题（H01–H04）全部通过闸门，新增决策点均以盲测反馈与比对报告的『偏离点细目』为证据；实战四题（F01–F04）修订均忠实于四份评估报告『Skill 修订建议清单』的结论（2024/2025 届获奖优秀论文全文未公开可读，获奖侧比对按『部分证据』降级处理，原始数据核验与题面口径为全证据链最高可信部分）。

---

## 二、题目分类器（拿到题面先定类，按顺序判断）

| 判据 | 图像视觉类 | 数据预测类 | 混合类 |
|---|---|---|---|
| 数据形态 | 图像/视频/信号波形/星图/遥感图 | 表格（多列数值）、时间序列 | 两者皆有（如视频+气象表格） |
| 任务动词 | 提取、识别、检测、匹配、测距、测速、定位、估计（物理量）、分割 | 预测、建模、分类、回归、筛选、构建、判别、优化、评估、描述分析（现状/趋势/挑战）、综合评价、政策评估 | 前段视觉估计+后段数据预测 |
| 输出形态 | 掩码/标记/识别结果/测量数值/几何量 | 预测值/排名/标签/优化方案/参数值 | 两类输出并存 |
| 代表题 | D01、D03、D04、D08（信号段）、D09 前段、H01（信号/阵列→STEP B′）、H04（栅格序列→STEP R）、F03/2025C（分割→STEP E；无标注→STEP G） | D02、D05、D06、D07、D08（表格段）、D09 后段、D10、D11、H02、H03、F01、F02、F04 | D09 |

**判类要点**：
- 有『图像/视频』即先挂图像类旗；D08 这类『信号+表格』复合题按小问分别归入两类 [D08]；2022A 这类『信号波形+定位』可直接归图像视觉类·信号子类 [H01]；
- 『估计物理量』既可能是图像类（由图像估能见度）也可能是预测类（由气象估能见度），关键看**输入数据形态** [D09]；
- 『描述分析/综合评价/政策评估』类动词落数据预测类（分析现状/研判挑战/贡献评估类走 F9/F10/M9/M13 通道）；『规则判定/确定性管道』类小问（不建模型、纯规则翻译）按 P2 处理并归数据预测类 [H02][H03]；
- **三类易漏归属的任务形态**（入口处先想一步，勿在定类时才回头补）：① 分割/像素级（掩码、IoU/Dice、标注转 mask、二值结果图）→ 图像视觉类，取 **STEP E**（有标注）或 **STEP G**（题面只给原图）；② 栅格场/多帧图像的『图到图』预报 → 图像视觉类，取 **STEP R**；③ 复指数叠加类（频率/DOA/距离-速度）→ 图像视觉类，取 **STEP B′ 谱估计层**，且禁用滤波去噪。
- **『按小问分归两类』与『混合类』的界分**（v1.5.2 补，原文未定义）：区别在**耦合强度**——① **分小问独立归类**（D08 型，如「信号段 + 表格段」）：各小问的输入输出**互不依赖**，分别走各自的树即可，**不构成混合类**；② **混合类**（D09 型）：前段小问的**输出即后段小问的输入**（「由图像估 X → 基于 X 预测 Y」），存在显式的**跨问数据流**，必须按 3.3 双轨并行处理。判据一句话：**后问用不用前问的结果？用 → 混合类；不用 → 分归两类。**
- **栅格场预报题的机理双轨入口**（v1.5.2 补，修 H04 双路由悬空）：题面为栅格场/多帧「图到图」预报时，主路走 **STEP R**（3.1 图像视觉类）；但若题面**同时给出领域机理公式/经验关系**（Z-R 类、雨滴谱、物理反演类），则**还必须进 3.3 混合类第二块**，取「显式物理反演 vs 物理衍生场 + 网络内嵌」的双轨对照——**该决策点由 [H04] 派生却住在 3.3，按分类器单向走会漏掉** [H04]。
- **『优化』动词的取法落点**（v1.5.2 补，消「无树可进」）：优化类小问**在第三节没有专属层**——其取法集中在**第四节第 4 步『求解』**（LP/QP/GA/PSO/DE/贝叶斯 + 帕累托前沿、反向优化的代理模型 + 启发式 + 数据定界、库设计的组合降元、DEA-BCC 标杆法、克里金空间插值）。故**定类仍按数据形态判**（通常落数据预测类），但**取法要跳到第四节第 4 步**，不要在三节里空找优化层 [D03][D07][D11]。

---

## 三、双方法论决策树

> 格式：**条件 → 选择 → 理由 → [溯源]**。遇到条件直接取用，不必重新论证。溯源编号：D01–D11（见顶部溯源纪律）；H01=2022A 定位、H02=2023D 双碳、H03=2023E 脑卒中、H04=2023F 降水（H 系列为 v0.9 盲测新增）；F01=2024C 磁芯损耗、F02=2024D 地理综合、F03=2025C 围岩裂隙、F04=2025D 低空湍流（F 系列为 v1.0 实战评估新增）。

### 3.1 图像视觉类（预处理 → 特征 → 谱估计 → 模型 → 评估 → 时空预报 → 分割专层 → 无标注实战层）

**STEP A 预处理层**

A1【视频扰动分诊：预处理主业随扰动类型切换，勿一套模型打天下】
- 条件：相机抖动/几何扰动 → 选择：先全局配准——特征点+RANSAC 估计帧到『参考帧』的变换（**非链式相邻帧**，防误差累积）→ warp 对齐 → **裁剪公共重叠区**（warp 黑边会污染背景建模）→ 掩码**逆变换回原坐标** → 理由：几何扰动摧毁『背景低秩』前提，不配准就减除必满屏误检 [D01]
- 条件：背景自扰动（树摇/水波/喷泉/窗帘）→ 选择：『先定位动态背景区域、再差异化处理』——GMM 多峰吸收 / 矩统计成像（MIF）/ 分解模型显式噪声项，三选一 → 理由：逐像素同质处理必败；三法功能同构，自创算子（MIF）是可拿的原创性层次 [D01]
- 条件：相机固定+背景静态 → 选择：背景减除族（中值/GMM/RPCA）+ 必配 Otsu+形态学+连通域后处理链 → 理由：后处理对整块掩码质量的决定性不亚于模型本身；横向对比 4+ 逐像素模型按场景选优，比只押单一理论主线更被评审买账 [D01]

A2【去噪工具包】
- 条件：信号类数据（脑电等）→ 选择：带通滤波（0.5–25/30Hz）+ EMD/DWT 去噪，并主动用 IMF/小波分量做『信号学叙事』（如证明 P300 存在于第 6/7 个 IMF）→ 理由：去噪与物理叙事双得分；EMD/DWT 缺席是知识盲区失分 [D08]；**适用前提（[H01]）**：仅适用生理信号等**非距离编码**信号；频率即信息量的信号（FMCW IF 等雷达/调制信号）**禁用滤波去噪**——滤波即毁掉频率编码的距离/速度信息，此类信号走 STEP B′ 谱估计处理
- 条件：雾霾/退化图像 → 选择：暗通道先验主流水线——暗通道（RGB 三通道最小值+局部最小滤波）最亮前 0.1% 像素均值估大气光 A → 粗透射率 t̂=1−ω·J_dark/A（ω≈0.95）→ 导向滤波细化 → 理由：场景结构先验，无需参考帧与目标检测；天空区注意规避 [D09]

A3【时间窗、采样与类别平衡】
- 条件：事件/刺激驱动的信号窗 → 选择：窗长与事件时程匹配（P300 取 150–650ms 而非 800ms），并做窗口消融实验 → 理由：长窗引入后续刺激污染与噪声维 [D08]
- 条件：类别不平衡（如 1:5）→ 选择：**过采样+数据增强**（正样本复制/叠加平均扩至 1:1）优先于欠采样 → 理由：欠采样丢信息；过采样即增强，是深度模型跑赢的关键推手 [D08]（表格类数据预测题同样适用，A3 非图像树专属）

**STEP B 特征层**

B1【不变量原理】条件：两组点集间存在未知旋转/姿态需建立对应（星图识别、点云配准）→ 选择：先枚举该变换群的不变量作合法特征空间（两两距离/夹角、邻域计数分布、序关系、属性差）；观测误差大时把**统计型聚合不变量**（环带/区间计数、直方图）放入粗筛层 → 理由：精确组合不变量区分度高但噪声敏感（容差两难：紧漏检、松碰撞——D03 星图 3 纯角距法失败即证据）；绝对坐标/固定方向角在未知旋转下不可比 [D03]

B2【时空中间表示】条件：视频事件检测（规律性/周期性/行进/扩散类）→ 选择：先构造 **xt 时空图**（掩码沿轴堆叠）→ 事件判别退化为图像几何/纹理分析（行进=线性排布、周期=等距条纹、逃散=发散扇形；FFT 频谱带宽判规律性）→ 理由：比逐帧特征+阈值更稳健且天然可画图；频谱阈值需在正常/异常样本双向标定 [D01]

B3【单目测距与测速】
- 条件：单目图像测几何量 → 选择：主线=『消失点/4 点对应求单应+已知参照物定尺度』；参照物优先级：**轴距>整车长/分段尺寸>路面标线>地砖/标准件**；参照物与目标**不同平面必须先做等高/竖直链传递**（等高面交比 h_c=H_ref/(1−CR)，或『传递测量法』逐级传递）→ 理由：轴距比整车长可靠（车头尾常遮挡/透视压缩）；不同平面直接透视变换必错 [D04]
- 条件：距离口径 → 选择：主输出三维斜距 √(D_horiz²+Δh²) 并附水平/垂直分量，口径显式声明 → 理由：拍摄者与目标不等高时三维斜距才是语义；口径不声明必失分 [D04]
- 条件：视频任务要距离 → 选择：**先测速（光流/帧差/车道线），再由『速度×帧数差=距离』**；优先找特殊帧（边界对齐、面平行、并排时刻）把几何降为最简形式（平行视角宽高比、直接差帧）；相对速度用免标定测法：已知长度物体完整通过视野，Δv=物体长/(帧时差/fps) → 理由：逐帧绝对测距误差随距离平方增长且差分放大噪声；测速优先是命题人明示导向，D04 最大方法层教训 [D04]；**适用前提（[H01]）**：仅适用**单目视觉**场景，不普适——雷达/阵列等并行估计传感器（FMCW：chirp 内测距、chirp 间测速，距离-速度并行估计）不适用，只能吸收『测速交叉验证轨迹』的精神
- 条件：标线/参照长度已知的几何测距 → 选择：用射影不变量语言（交比不变性、灭点灭线）而非显式相机内参标定；相机高度已知（如 6m）时一步到位 d(y)=fH/(y−y_vp) → 理由：推导干净、评委熟悉；标线不确定时用多标线段联合自洽求解，不赌单一值 [D09]

B4【多视角融合】条件：多相机看同一场景 → 选择：融合抬到 3D——体素占据推理（POM：体素占据后验 ∝ 各视角视线锥投影像素前景似然乘积），而非 2D 像素投票 → 理由：像素级投票是次优解；且第一步查题目下载链接的出处——**数据集链接本身可能是方法线索**（D01 的 EPFL 链接恰是 POM 作者实验室）[D01]

B5【通道/变量选择】条件：多通道信号 → 选择：嵌入式权重法三选一（注意力机制权重 / PCA 贡献率 / 组稀疏正则），个体最优集按交集/加权平均/频次 top-k 聚合出全局集，与领域生理规律互证（P300 主分布于中央-顶区与顶枕区）→ 理由：注意力权重零成本嵌入已有 CNN 且可解释；生理互证是加分叙事 → **陷阱**：全局交集可能缩到题设规模之外（如 10≤|S|<20），核心子集只作论证素材，必须补齐到合法规模再报 [D08]

B6【形态－形貌分离：何时必须去趋势】条件：定量目标是**局部起伏/表面形貌**（粗糙度、纹理、缺陷深度、波动幅度），而观测中还含由**已知几何关系、产状或成像姿态**决定的宏观成分 → 选择：**先剥离可参数化的宏观趋势项**（几何模型拟合、低秩/稀疏分解、多项式曲面），对**残差**做后续定量；报告须**并列**"未去趋势"的对照口径 → 理由：宏观成分的大梯度会主导一切**差分型指标**（斜率平方和、梯度统计、曲率度量），使形貌指标系统性虚高——实测同一批轮廓，未去趋势时指标约为去趋势后的 **2 倍** → 反例边界：若目标本身就是整体形态（倾角、走向、形状参数、尺寸），则**不得**去趋势，否则把待测量消掉；判据是"我要的差异相对于什么是局部的" [G]

**STEP B′ 信号/阵列谱估计层**（[H01]：2022A 盲测最大决策点空白——『从复指数叠加恢复频率/DOA/距离/速度』类任务）

S1【谱估计定阶】条件：信号模型阶数/目标数 K 未知（复指数叠加、多目标回波）→ 选择：特征谱信息论定阶——**MDL/AIC/**特征值 gap（大 gap=真实阶）为主，盖尔圆法（GDE）作等价备选；对 K 做敏感性分析 → 理由：MUSIC 类子空间算法必须先知 K，定阶失误影响极大（H01 与参考解析共识）[H01]

S2【子空间超分辨】条件：从复指数叠加恢复连续物理量（频率/DOA/距离/速度），或谱峰落在瑞利限以内 → 选择：**MUSIC/ESPRIT/root-MUSIC 子空间法**为主 + FFT 谱基线 + 多算法横向对比（RMSE 选优，对比表即得分点）→ 理由：FFT 分辨率受瑞利限约束，子空间法突破瑞利限；『多算法对比+量化选优』与无真值蒙特卡洛验证同构 [H01]

S3【相干信号解相干】条件：目标回波/信号间相干（协方差矩阵秩亏→经典 MUSIC/ESPRIT 失效）→ 选择：显式解相干——**前后向空间平滑 / Toeplitz 重构（MTOEP）/ FB-PTMR** 等；『快拍天然满秩免平滑』类论证只适用于特定快拍口径（快时间采样且各源频率互异），并须对『两目标距离接近瑞利限→快拍准共线病态』情形做解相干兜底 → 理由：相干→秩亏是雷达/阵列题第一难点叙事，解相干是获奖论文组织全篇的主轴 [H01]

S4【弱目标稀疏重构】条件：弱目标/低 SNR/欠采样/超分辨极限场景 → 选择：压缩感知（OMP/IHT/稀疏贝叶斯 SBL）作补充通道，与子空间法、FFT 基线三法互证 → 理由：CS/SBL 对模型误差更鲁棒（H04 并列：SBL 可作鲁棒化备选）[H01]

S5【阵列自校准】条件：阵列/传感器几何误差未知（老化、阵元位置偏差 δ）→ 选择：把**显式自校准**当一等解法类别——交替自校准 **ALS**（目标步：固定误差估计做超分辨定位；阵列步：固定目标位置用相位残差最小二乘估 δ，交替至收敛）+ 对角加载兜底；数据域抗差（子阵/滑窗/类平均）作工程备选 → 理由：自校准直接输出误差估计（可工程复校/给重校准周期建议），比纯抗差多一层输出物，且误差与角度耦合时非校准不可 [H01]

**STEP C 模型层**

C1【深度 vs 浅模型】条件：小样本（几百~千试次）信号分类 → 选择：不预设保守——1D-CNN（+BN+数据增强+早停）与浅模型（LDA/SVM）**同验证集横向对比** → 理由：『数据小→深度必过拟合』在 D08 被直接证伪（1D-CNN 77.87% vs SVM 68.17%）；『经典 vs 深度』对比表本身是评审偏好 [D08]

C2【分类 vs 回归叙事】条件：宽动态范围物理量的有监督估计（跨度 3–4 个数量级）→ 选择：先问『论文要展示什么指标』——能讲识别率就**非均匀分箱分类**（按业务重要性动态划分如 9 类、低能见度区间细分，Softmax+识别率+混淆矩阵讲故事）；要连续曲线就 **log 空间回归**（原始空间 MSE 被高值主导）；最佳=双轨对照 → 理由：获奖实践以分类+识别率（77.8%）为范式；分箱边界设计本身是论文设计点 [D09]

C3【大库唯一对应漏斗】条件：从大规模数据库为观测找唯一对应（星图/指纹/检索）→ 选择：三层漏斗——统计型粗筛（快、抗噪、库小）→ 精确几何复核（角距/距离闭环校验）→ **全局一致性终审**（解出变换后把全模板投影回观测空间，内点数+残差 RMS 一票裁决）；配多假设竞争+失败止损+显著性判据（max1>2·max2）→ 理由：单层精确匹配容差两难；终审层可把小问打通（D03：Q1 解算内核复用为 Q2 姿态验证探针）[D03]

C4【标注预算评估】条件：图像/视觉任务标注可得性未知（题面未明示有无标注）→ 选择：先做**标注成本-收益评估**再定标签策略——图像数<50 或单图标注<1h 量级 → 默认**人工标注部分图像+监督学习**（可加半监督），合成数据/弱标签降级为增强手段；图像量大/标注成本高 → 弱标签+合成注入+少量人工校验集；『标注预算=论文增分/时间成本』权衡写进方案 → 理由：小样本视觉题获奖主流是人工标注监督分割（2025C 附件图像量级小、人工标注 1–2h 即可覆盖主要裂隙），默认无标注走合成路线是对齐度降档的结构性缺口 [F03]

C5【参数化搜索的去重判据必须定义在观测空间】条件：用参数化模型族做检测/检索（Hough 家族：直线/圆/椭圆/参数曲线、模板匹配、曲线拟合、点集配准）→ 选择：**相似度定义在"观测空间的重叠度"**（候选曲线在容差带内的重合比例、候选区域 IoU、点集内点率），而非**参数空间的欧氏距离**；若两种空间都用，取**合取**（同时满足才判同一目标）→ 理由：参数相近 ≠ 解重合——实测同一目标的近似解在参数空间可分散到参数尺度的一大截（相对振幅尺度约 1/4、相位差数十度、偏移占图幅近 10%），参数空间阈值化与聚类都收敛不了；而单用观测空间又会把"空间上局部相交但实为不同目标"的两者误并，故合取最稳 [G]

C6【多目标枚举能力与归并能力不可互相替代】条件：一图/一帧内存在**多个同类目标**（多条线、多个圆斑、多个细胞/星点/缺陷，含弱目标）→ 选择：**枚举靠"检出即抑制"**（每检出一个即在邻域内衰减工作响应，再搜下一个），**归并靠 C5 的聚类判据**，两者都要有 → 理由：实测把"抑制"关掉、改成"先大量检出再事后聚类"，全部候选会退化成**同一个目标的近似解**（贪心搜索每轮取当前最优，未抑制时最优恒为最强目标，弱目标从未进入搜索视野），事后聚类只能在已检出集合内归并、**无法找回从未出现的候选**；反之只抑制不聚类，同一目标仍可能出现多个编号。功能正交：**搜索机制决定"能不能找到"，聚类机制决定"算几个"** [G]

**STEP D 评估层**

D1【无真值验证三层兜底】条件：无标注/无附件真值 → 选择：① 合成扰动注入（人工施加已知变换，算误差下界）；② 双模型交叉一致性（两原理不同模型输出对照+分歧帧复核）；③ 分布与消融分析（参数扫描、删模块退化量）→ 理由：数据缺失时最大化实验说服力 [D01]；图像测距另配：双通道交叉（两法/两参照独立测算，超阈值告警）+ 重投影残差（RMS<2px）+ 物理合理性软提示；**『无真值下的验证逻辑』写清楚本身是得分点** [D04][D09]

D2【评估三支柱+失败案例】条件：算法性能评估但无标准答案 → 选择：真实数据自洽指标（残差/内点比例）+ 蒙特卡洛仿真（可控注入噪声/缺失，带真值统计曲线）+ 消融实验（逐层关闭量化贡献）；**主动呈现失败案例**并分析机理 → 理由：只报『全部成功』式结果无效——失败案例+针对性改进更有说服力 [D03]

D3【阈值由分布形态驱动】条件：显著/异常帧判定类 → 选择：先画判定指标分布（直方图/KDE）看形态——多峰用 KDE 峰谷定界，单峰重尾用分位数/MAD+kσ → 理由：默认『噪声主导低分位』的单峰想象在 D01 被证伪（前景强度呈多峰）；固定阈值不可跨异质视频迁移 [D01]

D4【尺度相关指标必须连同尺度声明，并给尺度曲线】条件：用**随分辨率/采样密度变化**的指标描述图像（粗糙度、纹理度量、边缘/骨架密度、差分型指标、分形维数）→ 选择：报告值必须同时给出口径——**采样点数 N 或等效采样间距 + 滤波/分辨率尺度**；并把**尺度曲线**（指标 vs N 或 vs 尺度）与**幂律/分形指数**一并交付；跨尺度取极值或直接比较均无效 → 理由：此类指标本质是**多尺度测度**，实测同一批轮廓在采样密度由 10 增至 400 时指标从收敛值的 37% 单调升到 100%（约 120 点后收敛），幂律斜率对应分形维数 ≈1.2；不声明尺度则任何跨图/跨研究数字都不可比 [G]

D5【有效分辨率构成图像指标的物理下限】条件：从图像提取**物理量**（尺寸、宽度、粗糙度、位移、深度）→ 选择：**先做像素↔物理量标定**（题面几何关系优先，如视场/幅宽/标尺/已知参照物），再检查"待测物理量是否显著大于有效分辨率"；小于分辨率的细节**只能声明不可分辨**，必须用去噪/平滑尺度显式切断，不得计入指标 → 理由：分辨率是信息上限，把亚像素抖动计入指标只会得到"噪声"而非"形貌"——实测同一批轮廓，去噪尺度从 0 增加到约 1.2 mm 等效时，指标下降逾 50%（**预处理尺度的影响远大于采样方式的选择**，后者在本题仅差 0.5%）[G]

**STEP R 栅格场/图像序列时空预报层**（[H04]：2023F 盲测暴露——雷达/遥感/格点场『图像序列→图像序列』预报无归属）

R1【时空预测双轨】条件：栅格场×多帧×多通道→预测未来若干帧场（图像序列直接预测图像序列，非 D09 两段式）→ 选择：双轨裸比——物理/轻量轨（**光流外推/物理外推**、机理公式基线）与深度时空轨（**U-Net/3D U-Net、ConvLSTM**）同验证集对比，深度模型作主线、机理作基线；多高度面/多层次分别建模并对比（分层建模是设计点而非默认单层）→ 理由：深度时空外推通常是命题人文献线索主线（文献聚类≥3 篇同族即验证）；预报题『对象/样本』语义按过程/网格/高度面重新界定（见 M4 补充）[H04]

R2【生成式缓解模糊】条件：深度时空回归预报出现『回归到平均』（输出模糊、峰谷被压平、高值区技能掉）→ 选择：**cGAN**（生成器=已有最优主干 + patch 判别器 + 谱归一化/梯度惩罚）或 GAN 数据增强；对照后处理（拉普拉斯金字塔细化/直方图匹配）；评估加频谱（功率谱斜率）与锐度指标，『锐利但失真』用 FAR 监控；若增益≈0 如实报告 → 理由：生成式对抗是缓解模糊预报的标准路线（与获奖论文 GAN 族同源）[H04]

R3【预报场评估指标库】条件：预报/预测场质量评估 → 选择：业务技能分 **CSI/POD/FAR** + **Bias** + SSIM/MSE 齐上，按强度档（如 20/35/45dBZ）与过程类型分层报告，配功率谱斜率/锐度；不报单一总体指标 → 理由：只用 MSE 会掩盖高值区低技能与模糊化失真 [H04]

R4【时空场时间维平滑】条件：三维场/栅格场含时间序列（多源融合、插值后处理）→ 选择：标配**时间平滑/递推**——一阶卡尔曼递推（或指数平滑）压制帧间闪烁 + 半拉格朗日/光流平流一致性检查；融合题默认交付『空间+时间』双平滑 → 理由：2025D 获奖融合路线标配卡尔曼递推+半拉格朗日平流抑制时间闪烁，只做空间插值丢时间维一致性是结构性差距 [F04]

R5【集合预报形态】条件：预测/短临外推类题做不确定性量化 → 选择：**集合预报**——多成员生成（多初值/多模型/扰动：幅度 ±10–20%、方向 ±10°）→ P10/P50/P90 分位聚合+区间输出；与去相关尺度/预测区间法并列，输出形态**优先集合**（可视化友好、直接服务下游决策）→ 理由：2025D 获奖路线用集合扰动输出不确定性区间、工程直接，且直接支撑航路稳健性决策；骨架见 `templates/ensemble_forecast.py` [F04]

**STEP E 分割/像素级任务专层**（[E]：2026-09-10 真实数据实跑实证——crack-seg 4029 张裂缝图（train 3717/val 200/test 112，416×416，YOLO 多边形标注）A 方案全流程；脚本与产物路径见 `CHANGELOG.md` v1.2『实测数据存档』）

E1【标注格式转 mask 口径】条件：标签是几何图元（YOLO txt 多边形、COCO polygon、labelme 折线/多边形）→ 选择：**转换口径显式声明 + 一致性检查**——多边形填充（`ImageDraw.polygon(fill=1)`，本次实跑采用）或线段宽度膨胀（细裂缝场景的另一口径）；转换完立即统计**正样本像素占比**（本次实测 train 2.08% / test 1.94%；fill 与 stroke(3px) 两口径占比相差 0.41 个百分点 ⇒ 口径不声明就是基准漂移）与单图 mask 覆盖率，与标注说明/题面口径对照，口径写入报告方法节 → 理由：同一份标注换口径可让 Dice 基准相差数个点，口径不声明=结果不可复现、不可横向对比 [E]

E2【分割评估口径：Dice/IoU 为主，PA 不作主指标】条件：正负极端不平衡的像素级评估 → 选择：主指标 **Dice + IoU**（另报 Precision/Recall），**PA 只作辅证**并注明不平衡前提——本次实测 **PA 0.9899 vs Dice 0.7377**（背景约 98%，PA 看似『接近完美』却完全掩盖细裂缝漏检；另注：实测脚本原 PA 公式分母口径写错曾得 0.6566，修正后 0.9899——**指标公式本身也要写清**）；同时**声明指标聚合层级**：本次实测**逐图 Dice 均值 0.7196 ± 0.1668** vs **批级 Dice 0.7377**（差 1.8 个百分点）→ 理由：指标混用使横向对比失效，评审口径必须写清 [E]

E3【逐图指标分布 + 最差样本归因】条件：分割/检测类测试集评估交付 → 选择：均值之外必须交付**逐图指标分布**（直方图/分位数）与**最差样本可视化归因**（三联图：原图｜真值｜预测叠加，命中/误差着色）——本次实测 112 张逐图 Dice：P10 0.586 / P50 0.756 / P90 0.863，**左侧长尾 6 张 <0.4**（最差 2 张 Dice **0.000** 完全漏检、第 3 张 0.111，均为阴影/剥落粗块与低对比度区），全被均值 0.72–0.74 掩盖 → 理由：只报均值会隐藏系统性失效模式；最差样本是评审追问点，也是改进清单的直接证据来源 [E]

E4【从头训练收益天花板与早停纪律】条件：无预训练权重、中小规模数据上的轻量模型 → 选择：**early stop patience=8 + 保存 best 权重**，把剩余预算投向升级清单——本次实测 val Dice ep1 0.676 → ep3 0.736 → ep15 峰值 0.753 → 其后到 ep23 仅 +1.7 个点（平台），早停省时且避开末期波动（ep23 0.746 < ep15 0.753）→ 理由：从头训练在小数据上快速饱和，继续训练是预算浪费，收益应投向架构/编码器/数据增强 [E]

E5【分割基线参考值与升级清单】条件：需判断『模型够不够用、还差多少』→ 选择：对照本次实测基线——**416px + 轻量 U-Net（4 层 16/32/64/128，1.94M 参数）+ BCE+Dice + Adam 1e-3 Cosine + AMP + batch 16**，3717 张训练 → **test Dice 0.7377 / IoU 0.5844 / P 0.7399 / R 0.7354，23.1 分钟，峰值显存 2074 MB（RTX 3060 Laptop 6GB）**；按阶梯投入升级：① 预训练编码器（ImageNet/大规模自监督）② 多尺度/高分辨率 patch（保细裂缝）③ TTA（翻转/多尺度）④ 更强增强与损失（Focal/Tversky/边界损失）⑤ 深监督 ⑥ 减小下采样倍数（8× 下采样会吃掉 <3px 细裂缝）→ 理由：给出可比基线与升级阶梯，避免『无参照地盲目加深训练』或『误判已达上限』 [E]；骨架见 `templates/segmentation_eval.py`（逐图指标+分布+三联图）与 `templates/polygon_to_mask.py`（标注转换+一致性检查）

**STEP G 无标注/弱标注图像任务实战层**（[G]：2026-09-10，2025C 官方附件 1 十张钻孔展开图 244×1350 **无标注**实做问题 1；脚本与产物路径见 `CHANGELOG.md` v1.3『实测数据存档』；与 STEP E 的区别：E 是有标注监督分割口径，G 是**题面只给原图**时的方法与验证）

G1【物理标定先行】条件：图像含物理量纲线索（钻孔孔径、管道内径、遥感幅宽、标尺）→ 选择：**动手检测前先建立 像素↔物理单位 标定**，题面给出的几何关系优先——本次实测：展开图宽度 244 px 对应钻孔周长 π×30 = 94.248 mm ⇒ **0.3863 mm/px**；题面判据『张开宽度 >1mm』⇔ **≥2.59 px**，据此确定滤波尺度范围（σ=1.3–3.5）、最小宽度约束与掩码宽度 → 理由：没有标定时『宽度/尺度/阈值』全是拍脑袋参数，无法写入报告也无法跨图复用；标定一次即把物理判据转成像素判据，评审可核验 [G]

G2【无标注任务：题面公式即主模型，走参数化搜索】条件：像素级识别任务**无标注**（题面只给原图）→ 选择：不要把『无监督』默认理解成『聚类/阈值/连通域』——先找题面是否已给出**形态方程**，有则直接做『**参数化模型 → 参数空间搜索 → 物理判据筛除**』：本次题面公式 y = R·sin(2πx/P+β)+C 即为裂隙形态模型，用三维参数搜索 (R,β,C)、P 固定为周长（题面给定）→ 理由：题目公式是最强先验，比任何通用无监督方法都贴合命题人意图；反过来若只做阈值+形态学，断裂细结构必然检出碎片化（实测 v1/v2 掩码断裂、按连通域拟合出 R=0.1mm 的假正弦）[G]

G3【参数空间去重失效 → 改用像素空间邻域抑制】条件：Hough/模板匹配类参数搜索产生大量近似解 → 选择：**迭代贪心提取 + 每检出一条即抑制其邻域响应**，不要指望参数空间 NMS 或聚类去重——本次实测同一条裂隙的近似解在参数空间分散达 ΔR 60 px / Δβ 50° / ΔC 100 px，参数空间 NMS 只从 30 条降到 26 条、聚类降到 11–15 条且不收敛；改邻域抑制后一次收敛到 6 条，与人工复核的 6 条一致 → 理由：参数相近 ≠ 曲线重合（同一暗带的容差可容多组参数），去重判据必须在**成像空间的实际重叠度**上定义 [G]

G4【逐图归一化响应 ⇒ 阈值必须图内自适应】条件：多图批量走同一流程、且特征响应做过逐图归一化 → 选择：**禁用固定绝对阈值**，改 `阈值 = max(下限, 比例 × 该图最高分)`——本次实测图间最高响应相差 **5 倍**（图1-2 0.331 vs 图1-3 0.068），固定阈值使 **4/10 张整张零检出**；改自适应后 10/10 张有输出 → 理由：逐图归一化消除了绝对尺度却保留相对结构，固定阈值等价于对弱响应图宣判漏检；本条是 D3【阈值由分布形态驱动】的『**跨图**』推广（D3 管单图内的分布形态，G4 管图与图之间的尺度差异）[G]

G5【无标注 + 无参考解析：视觉复核即验证通道，且必须驱动迭代】条件：真实赛题数据、无标注、无参考解析可比 → 选择：在 D1 三层（合成扰动/双模型交叉/分布消融）之上补『**逐图视觉复核 → 缺陷归类 → 规则化修改**』闭环，并把每轮发现写成消融证据——本次 v1→v5 每版缺陷都由复核指出并转化为规则：只检暗脊⇒亮色充填裂隙漏检（加双向 Frangi）、掩码断裂⇒拟合假参数（弃连通域拟合改参数搜索）、重复检出（加邻域抑制）、边界伪影（加边界约束）、固定阈值失效（改自适应）→ 理由：无 GT 时人工/视觉复核是唯一能发现**系统性失效模式**的通道；关键是复核结论要可追溯地转成修改（而非随机调参），并把消融表交给评审 [G]

> **〔v1.6 注·容差带打分的正确用法（T64 落点）〕** 本层 G2/G3 与模板 `sinusoid_hough.py` 均推荐
> **容差带内打分**（±tol 带内取最大响应作候选得分，优于单点响应）。**该推荐仍然成立，但必须与
> 「用带内 argmax 提取观测点」严格区分**——两者形式相近、后果相反：
>
> | 用法 | 做法 | 判定 |
> |---|---|---|
> | **检测/打分用** | 带内取最大响应，作为**该候选的得分** | ✅ 稳健，保留（G2/G3 即此） |
> | **提取/拟合用** | 带内 argmax 的**行位置**当作"观测点"，再据以重拟合或算 R²/MSE | ❌ **T64 陷阱** |
>
> **理由（2025C 问题2 实测，66 条裂隙）**：① 带宽容差 ±10px → ±25px，R² 均值 0.9563 → 0.8567，
> **66/66 条全部变差**；② 危害**随响应强度单调衰减**（按得分分箱降幅 0.1049 → 0.0797 → 0.0176，
> 弱响应区约为强响应区的 **6 倍**）——宽窗 argmax 取到的是**噪声极大值**，而 E[max of N 噪声] 随窗宽增长；
> ③ 再用该响应值当最小二乘权重，两个带宽下**一致变差**（−0.0168 / −0.0325）——权重本应 ∝ 1/σ²，
> 而它是 argmax **选出来**的最大值，等于给噪声最大的点最高权重。
>
> **两条纪律**：**带宽必须双报**（窄带因"贴着拟合曲线取样"自证偏高、宽带因噪声极大值系统偏低，
> **两个都不安全**，且二者差异本身就是识别噪声机制的判据）；**权重不用响应值**（用与选择过程独立的量，
> 或径用无权最小二乘）。详见 `陷阱库.md` **T64**，原始现场 `q2_fit_quality.py` 与 `work/q2_fitq/`。

### 3.2 数据预测类（数据审计 → 特征工程 → 模型选择 → 验证 → 不确定性分析）

**STEP 1 数据审计**

P1【混叠优先原则】条件：离散/稀疏采样+周期信号提取（卫星、浮标、巡测）→ 选择：建模前先做采样混叠分析——信号频率×采样周期 → 表观混叠周期表；Rayleigh 判据（T_min≈1/|Δf|）给出每对信号可分所需资料长度 → 理由：题目『能否有效提取取决于资料长度』类提示考的就是混叠；联合拟合只克服分潮间能量泄漏，真正分辨靠记录长度 → **陷阱**：频率差必须取折叠到 [0,Nyquist] 后的绝对值差（带符号相减会把 M2/S2 需 ~3 年算成 30 天）；专查混叠周期与低频信号近邻组合（172.8d vs 182.6d）[D02]

P2【清洗规则参数来源】
- 条件：清洗阈值设定 → 选择：**题面每个数字逐条翻译成判废/修复规则**（D05：7s→加速度上界、7.5~8→减速度下界、10km/h→爬行转怠速、180s→怠速截断、120km/h→超速判废）；领域默认值记牢：状态分类阈值 0.1 m/s²、PCA 累计贡献 85%、片段最短 15s 量级 → 理由：题面数字即阅卷口径；自造阈值=口径漂移，除非有数据分布论证 [D05]
- 条件：管道式清洗 → 选择：题目给定规则+稳健备选+顺序留痕（删位点→补全→限幅→3σ 迭代（贝塞尔 σ）；分布未知补 5/95 百分位截尾或 MAD 备选）→ 理由：3σ 依赖正态假设，重尾需稳健备选；顺序留痕供审计 [D07]

P3【清洗即超参数：阈值-误差反馈闭环】条件：海量点位各自序列长度不一 → 选择：把『最少观测数/有效性』阈值当可调超参数扫描（如 0/50/100/…/500），以**最终任务精度**（而非清洗阶段统计量）反馈定值 → 理由：一次性统计规则漏掉『短序列既不稳定又不可分辨（混叠）』的物理因素；低成本亮点章节 [D02]

P4【联合视图清洗+分布一致性】条件：训练/测试分表 → 选择：一切以列存在性/常量性为基础的删除决策基于**训练∪测试联合观察**；建模前用 KDE 对比/KS 检验剔除训练-测试分布漂移显著的特征 → 理由：训练集全 0 的列测试集可能非 0，删除即缺维度预测崩溃；漂移特征让精度系统性高估 [D11]

P5【缺失与异常分层】条件：时间序列缺失/异常 → 选择：特征侧缺失必须插补（拉格朗日/均值，**时间序列缺失不能直接删除**，滚动特征与自回归链要连续序列）；目标侧缺失样本足时剔除、不足时插补并标记；负浓度等物理异常直接剔除；箱线图检出离群后结合背景人工甄别（保留合理极端值）→ 理由：异常值环节是各获奖方案共有的标准预处理块 [D10]

P6【采样率核对】条件：多列高频数据成样 → 选择：成样前按位点核对采样间隔——工业数据常混频（3min 与 6min 并存）→ 理由：『约 40 节点=2h』类推断必须用时间戳验证而非默认 [D07]

P7【审计交付物】条件：『给出处理后记录数』类要求 → 选择：输出规则归因审计表（原始→每规则删改数→最终数，勾稽平衡）；若数据本就无坏值，如实报告『验证无需修改』→ 理由：无唯一正确数字，评的是规则合理性与自洽性；诚实零差异比强行清洗更得分 [D05][D07]

**STEP 2 特征工程**

F1【选原始变量 or 降维】
- 条件：题干要求『选出前 N 个原始变量/描述符并排序』→ 选择：**禁用 PCA/因子分析作主降维**（主成分不可解释、不对应原列）；用过滤法+嵌入法多方法交叉，可解释性=筛选合理性的核心得分点 → 理由：D11 多来源共同强调的红线 [D11]；**适用前提（[H02]）**：仅适用『从原始变量中筛选』类任务，『设计指标/构造指标』类小问（一(1) 型）不适用、走 F9
- 条件：允许/需要降维补充信息 → 选择：两段式——先文献/机理锁定核心变量『原则性保护』（化工题：温度、压力、氢油比、空速类），PCA 只处理其余变量作精炼指标，主成分不取代保护变量 → 理由：清单永远含可操作的工艺主旋钮 [D07]

F2【两阶段粗筛-细筛】条件：高维变量筛选 → 选择：树模型重要性（RF/LightGBM/XGBoost+SHAP+permutation 双口径）粗筛出约 2 倍候选 → 相关性/共线性细筛（Spearman/MIC，|r|>0.9 组内删一）→ 独立性检验与理化论证**置后**；连续/离散变量分组筛选；bootstrap 报排名稳定性 → 理由：『粗筛→细筛→检验置后』是多来源共同证实的标准范式，检验置后避免误删重要变量 [D11]

F3【相关性口径】条件：非正态数据 → 选择：先做 JB/正态性检验，非正态则 **Spearman 为主口径**+MIC（非线性），Pearson 仅参考 → 理由：秩相关对非正态稳健 [D11]

F4【极坐标参数对】条件：出现 (H,g)/(A,φ) 类极坐标参数 → 选择：x=H·cos g、y=H·sin g 线性化+最小二乘；**后续一切分析（拟合/插值/分离/检验/绘图）在分量域进行**，最后一步转回 (H,g)；相位时区基准最先统一 → 理由：避免非线性优化初值问题；直接对迟角 g 插值在 0°/360° 缠卷处失败 [D02]

F5【因子筛选排序是评分点】条件：『哪个因子影响最大』类问题 → 选择：交付显式流程——自变量相关矩阵（>0.9 去重）→ 与目标相关排序保留 top3-5 → 显式回归方程（系数+显著性）→ 重要性双源对照（Pearson/RF）→ **系数符号物理一致性检验**（湿度↑→能见度↓；静稳成雾/大风消雾的非单调性讲清）→ 理由：筛选流程是可展示中间产物、排序是隐含评分点；黑箱不交关系式违背『导出具体关系式』字面要求 [D09]；若题目背景自带业务传导链（如脱硫→烯烃→RON 损失），补中介效应检验把『变量级』解释升级为『机制级』证据（Baron-Kenny 逐步法即可，低成本高辨识度）[D07]

F6【特征做满】条件：低维特征题 → 选择：对数/比值/组合/均值一起上（如能量占比 θ+δ/α+β、逐带 lnE，4 维做满到 9–13 维），控制 ~10–15 维防过拟合 → 理由：方向一致但清单偏窄=深度不足失分 [D08][D10]

F7【字段盘点】条件：多源传感器数据 → 选择：先盘点全部字段——经纬度→轨迹可视化+城市判定（轨迹图叠加速度着色是公认可视化亮点）、转速→熄火判定、CAN 类字段→扩展特征库、三轴加速度噪声大时弃用改用平滑车速差分 → 理由：只用速度单字段既损失信息又丢工作量展示 [D05]

F8【聚类锚点】条件：气象条件/类别划分题 → 选择：**以污染水平为锚的聚类**——先验证季节性 → 相关验证 → 分季节对气象指标 PCA（如春/夏/秋 3 主成分、冬 2）→ 主成分与 AQI/IAQI 联合聚类 → 按类内 AQI 均值排序阐述『第几类=什么天气型=平均 AQI 多少』；分季节是一等设计步骤 → 理由：类间差异成聚类固有输出，直接命中『按影响程度分类』 [D10]；**迁移注意（[H03]）**：锚可以是自构造量（如轨迹形态特征，2023E 2b 型）而非必须题面外部变量——保持『锚使类间差异成为固有输出』即可

F9【指标体系构建】条件：『建立/设计/构建指标体系』类小问（非变量筛选）→ 选择：**指标选取三原则**——**政策锚点驱动**（每个指标数值与定义来自题面或政策文件、可溯源，不发明政策数字）、**可计算**（每项指标给出计算式与数据来源）、**口径可核对**（单位/基期/时点全篇统一并在口径表钉死）；结构上用『总量-结构-强度-部门』四层 + 恒等式串接（Kaya 类 C=P·g·i·f，或结构式 i=Σwₖiₖ） + 可预测性标识 → 理由：指标题评的是结构自洽与锚点正当性，罗列式必失分；此类小问**不适用** F1 的『筛选』语义 [H02]

F10【指数分解与影响分析】条件：『分析影响因素及其贡献』类小问 → 选择：**LMDI 加和分解**（对数平均迪氏指数，人口/经济/能效/结构四效应）+ **残差分配**（加和分解天然无残差并显式自证）+ 部门双视角；工具选型按题目字面：要**贡献值**→LMDI/结构分解，要**显著性**→ANOVA/方差分析，要**关联强度**→灰色关联 → 理由：『贡献』字面要求精确贡献值，LMDI 直给且无残差；ANOVA 只给显著性（H02 偏离根因）[H02]

**STEP 3 模型选择**

M1【默认族不预设】
- 条件：表格/时序预测 → 选择：线性族、RF/XGB/LGBM、SVR、BP、Stacking **全部进嵌套 CV 裸比** → 理由：『小样本必线性』是错误先验（D07：325 样本撑起非线性集成，MSE≈0.03 量级）；Stacking 增益可能≈0，如实报告 [D07]
- 条件：低维强交互表格分类（4–13 维）→ 选择：**GBDT 家族第一顺位**（CatBoost/XGBoost/LightGBM）→ 理由：D08 实证 CatBoost 95.06% vs SVM 65%（30 个点差距）[D08]
- 条件：QSAR/活性回归 → 选择：GBDT 族为精度主力，线性只作基准与可解释锚点；SHAP/Sobol 敏感性承担『变量-活性方向』论证 → 理由：实测 R² 0.77 vs 0.5；可解释性不必硬绑线性牺牲精度 [D11]
- 条件：数值序列+时间结构（周期/时序序列→标量回归或预测）→ 选择：**序列模型与树模型双轨并列进动物园**——LSTM/BiLSTM/GRU/CNN-LSTM 具名入主线候选，与 GBDT/树家族同验证集裸比；1D-CNN 仅作通用对照 → 理由：2024C 冠军主线即『多因素交互双向 LSTM 跨材料多工况预测』，周期序列输入（如 1024 点 B(t)→标量回归）天然适配序列网络，深度序列模型只放对照轨是结构性缺口 [F01]

M2【残差学习范式】条件：二次建模/误差订正题（有一次预报基准）→ 选择：学习『一次预报→实测』系统偏差并叠加修正量（或一次预报作最强特征端到端建模）；**输出前先做口径对齐**（O3 日最大 8h 滑动平均、其余 24h 均值）再算 AQI 与首要污染物 → 理由：多来源一致的默认范式 [D10]

M3【预测粒度】
- 条件：目标单日、数据逐时 → 选择：**先逐时预测 72h 再按附录口径聚合**（O3 取 8h 滑窗、其余 24h 均值）→ 理由：样本量×24、O3 的 8h 非线性折线误差路径与口径天然一致；日级建模仅作快速基线 [D10]
- 条件：视频-标签学习 → 选择：15s 抽帧+缓变假设为主链；帧级融合（3D-CNN/LSTM）作增量实验（先跑单帧基线、再用增益对比呈现），不作主模型 → 理由：轻量 CNN+标签对齐是获奖队通用策略；时空模型无获奖背书时先证明增益 [D09]

M4【同构不同参】条件：题设『模型同时适用于多个对象/站点』→ 选择：同一模型结构、**逐对象独立训练**（权重不同），切勿池化混训 → 理由：对象间分布不一致，池化拟合『平均对象』稀释精度（一等奖核心经验）；池化仅样本严重不足时作备选且须同协议对比 [D10]；**适用前提补充（[H04]）**：深度学习池化训练（图像/栅格场序列类）时『对象/样本』语义需按赛题重新界定——对象=站点/独立单元时逐对象独立成立；对象=序列片段/降水过程/空间网格时池化是领域主流（数据规模红利），应以『按对象类型分层评估』替代独立训练，勿机械套用

M5【分治晚融合】
- 条件：多源异构数据（不同粒度/列数）→ 选择：各源独立建模（如逐时预报 21 列模型、逐时实测 11 列模型、逐日实测 6 列模型）输出融合（平均/加权/Stacking）→ 理由：各源的统计特性/缺失模式/粒度差异在独立模型内消化；早融合（特征拼盘）次选且需超配特征工程 [D10]
- 条件：多法/多源并列（同族≥3 或题面三法并列）→ 选择：**融合优先于单选**——默认『多指标综合+主法标注』：加权最小二乘/最优插值（OI≈3DVAR 近亲，权重取 1/σ²）+ 光滑先验 + 一阶卡尔曼递推时间平滑；单法深挖仅当数据充分保证标定可行（依赖装置常数/标定系数且缺附件的单法风险更高）→ 理由：2025D 获奖路线『多指标综合诊断+加权融合』优于题面三法选单法深挖（功率法 Cn² 依赖装置常数标定，缺附件不确定性大）[F04]

M6【组合预测+多模型对比】条件：预测题定稿 → 选择：构建 2+ 模型（ARIMA+LSTM、RF+ARIMA/Prophet 双通道、灰色+SVR 等）统一指标体系横向择优 → 理由：组合抓不同结构（趋势+非线性残差）；『多模型对比实验本身是独立得分点』[D06][D05]；组合定权细节：优化定权（PSO 类）/熵权/简单平均三选一并声明，勿只说『加权』[H02]

M7【任务结构决策】条件：多标签/多输出任务 → 选择：拆为 k 个独立二分类而非多标签大模型；**逐标签分别选型**；不均衡三件套（折内过采样/Focal Loss/类别权重+阈值移动 Youden J），评估 AUC 为主辅以少数类 Recall → 理由：独立任务可分别匹配最优模型（D11 实测 XGBoost 合 3 任务、RF 合 2 任务）[D11]

M8【范式转化】
- 条件：看似 N 类的大任务 → 选择：检查『降维打击』结构——36 字符识别 ≡ 『该行/列闪烁是否含 P300』二分类（1:5）+ 行列 argmax 交叉定位 + 多轮投票（软融合得分平均/硬融合『与之前某轮一致即中』）→ 理由：P300 对字符本身无区分性，直接 36 类建模是题意核心误读 [D08]
- 条件：半监督机会 → 选择：优先找**实验设计先验**构造结构伪标签（每轮每行每列恰一次正样本 → 取行列得分最高者为伪标签），配无先验门控（softmax>0.8）对照；结构约束+门控+早停防错误累积；备选半监督判别分析（免迭代）→ 理由：结构先验半监督逼近全监督（63%→71.48% vs 75.2%）[D08]

M9【情景-核算-目标判定闭环】条件：政策/规划类题『≥N 个情景 + 多情景核算 + 目标达成判定』（双碳/路径规划/规划评估类）→ 选择：三步闭环——① **情景参数化**：情景三元组（达峰年/中和年/核心驱动参数 i(t),b(t)）由政策锚点+插值给出，含自然情景对照（可加『滞后』情景凸显干预必要性=加分项）；② **核算矩阵**：部门×品种×因子核算矩阵 + 间接排放口径（『谁消费谁负责』折算）+ 『自上而下=自下而上』双轨一致校核（恒等式回代=免费真值）；③ **目标判定**：对标尺取题面/政策数字（如 2060 排放≈基期 20%、强度降至 5%），加容差带 + 情景单调性 + 灵敏度（Sobol/蒙特卡洛）→ 理由：情景-核算-判定是政策题完整模块，缺任一环即『骨架级有效、血肉级缺失』（H02 偏离根因）[H02]

M10【重复测量/纵向数据】条件：同一对象/患者多时点观测（纵向/随访/面板数据）→ 选择：**混合效应模型 LME**（随机截距/随机斜率）为主 + GAM/饱和曲线三轨裸比（信息准则选型；算力预算有限时 CV 只跑终选）；**对象级（患者级）交叉验证**——记录级切分产生近邻相关泄漏，精度虚高；轨迹形态（斜率/达峰时间/峰值等）聚类分亚组，**先证个体异质性方差占比（存在分组结构）再聚类**；**聚类事实优先：机理用于解释结构、不用于否定结构**（[H03]）→ 理由：纵向观测当独立样本=伪重复（H03 2a/2b 偏离根因）[H03]

M11【有序分类目标】条件：目标是有序等级（mRS 0–6 类、等级评定类）→ 选择：**累积链接模型/有序逻辑回归**（比例优势假设检验）/回归取整三轨对照；评估用 **QWK（二次加权 Kappa）**+±1 邻近准确率（领域惯例），不用名义准确率；极端档样本 <5 时合并邻档讨论 → 理由：丢弃有序性=丢结构信息；M7『拆 k 个二分类』只管名义多类，有序目标需单独选型 [H03]

M12【观察性混杂控制】条件：观察性数据 + 处理/治疗变量（按指征给予、非随机分组）→ 选择：混杂控制三件套——**倾向评分**（PS 匹配/分层/IPW 加权）+ **混杂变量调整**（协变量+交互项如 Treat×时间）+ **多重比较校正**（BH-FDR）；**因果方向显式假设**（机理先验可作方向/结构约束，如『血肿→水肿』定箭头）；解读顺序：反直觉结论先查混杂再怪模型 → 理由：指示混杂下『治疗组更重』≠治疗有害，不控制则结论公信力崩（H03 2c 偏离根因）[H03]

M13【综合评价方法库】条件：综合评估/贡献度排序/方案优选类小问（多准则决策语境）→ 选择：**AHP（层次分析）/熵权法/TOPSIS** 多准则决策库——客观权重→熵权，主客观结合→AHP+熵权，排序→TOPSIS；与实验归因（消融/置换/Shapley）**双轨互证**；探索性因素分析问（无监督探索）显式声明 F1 红线不适用 → 理由：综合评价体系是竞赛评阅熟悉写法（获奖论文常态），只会实验归因则与评阅预期不同族（H04 Q4、H03 3c 部分对齐根因）[H03][H04]

**STEP 4 验证**

V1【嵌套防泄漏协议】条件：高维小样本（n≪p）→ 选择：特征选择与调参全在训练折内执行；工业时序按时间切留出；指标注明切分口径 → 理由：随机切分下相邻样本高度相似会虚高精度；警惕无切分说明的『99% 准确率』[D07]
- 条件：存在起报时刻/时间因果 → 选择：建立『特征时间戳 ≤ 起报时刻』检查表；滚动重训模拟逐日节律 → 理由：信息泄漏比模型选错更致命，是隐形评分门槛 [D10]

V2【双闭环检验体系】条件：结论需自证 → 选择：内部一致性（同一物理场两套观测几何=『免费真值』：轨道交叉点、前后半段重估）+ 外部独立真值（验潮站类）；外部检验误差**按离岸/水深/振幅分组归因** → 理由：近岸代表性误差（16cm 级）会主导外部检验而被误判为方法误差（交叉点仅 ~1cm）[D02]

V3【达标判定口径】条件：达标率类结论 → 选择：实测基线+测量误差容差带+分层报告（达标/边缘/不可达+注明激活约束）→ 理由：判定对基线/模型口径高度敏感——同一样本不同口径降幅可差一倍（31.61% vs ~65%）[D07]

**STEP 5 不确定性分析**

U1【阈值时刻给置信区间】条件：『何时达到阈值』类预测 → 选择：曲线拟合→数值求根（如 MOR(t*)=150m）只是及格线；参数 bootstrap 给 t* 置信区间，上游估计误差级联传播；诚实声明『预测真值也是估计值』→ 理由：不确定性量化是优秀论文深度的分水岭 [D09]

U2【误差源清单决定结论】条件：选星/布站/布阵/构型最优类 → 选择：**先穷举误差源再下结论**——只有随机噪声→条件数/张开度最大化；存在随位置增长的系统误差（径向畸变、大气折射）→靠近误差小区域；并存→联合误差模型求折衷；每个理想化假设回扫『结论对其敏感性』（与空间位置耦合的假设优先级最高）→ 理由：D03 只算随机误差得『边缘张开』、论文加畸变得『靠中心』，各自都对又都不全 [D03]

U3【开放题闭环】条件：开放讨论/提出新概念题 → 选择：『**概念→可计算指标→未来预测**』三层闭环（概念覆盖趋势性+复杂性；指标给计算式；再预测指标走势）；『对前问有何启示』类给多准则证据链+一条可操作反馈路径（把 1D 结论翻译成下游插值超参数或数据加权规则）→ 理由：纯文字口号是失分形态；孤立实验拿不到『启示』分 [D06][D02]

### 3.3 混合类（D09 范式 + H04 衔接）

- 条件：题目分两段『由图像估计 X』+『基于 X 预测 Y』→ 选择：双轨并行——估计段走 3.1 图像树（暗通道先验+几何测距+σ 换算；**终点唯一：估计大气消光系数 σ**，σ=−ln t/d，MOR=ln(1/ε)/σ，ε=0.05 时 ≈2.996/σ，全篇统一口径）；预测段走 3.2 预测树（趋势拟合外推+阈值求根+bootstrap CI）；跨问联动作条件性加分（有气象观测时用 Q1 模型修正消散速率）→ 理由：混合题按小问各归其类；换算常数写错（2.996 vs 3.912 混用）直接全盘扣分 [D09]
- 条件：题面含领域机理公式/经验关系（Z-R 类、物理反演类），且主建模为数据驱动 → 选择：**先问『有无可用机理公式』**——有则『显式物理反演』（公式拟合/反演子模型：雨滴谱反推、模糊逻辑相态、回归含水量类，输出可审计的物理量）优先作『建模深度』叙事，『物理衍生场+网络内嵌』作并列替代方案；机理公式作基线/初值/软约束，数据驱动作主线，**同指标双轨对照**（机理拟合与深度回归在同一验证集上比）→ 理由：显式物理反演比纯网络内嵌更易得评阅『建模深度』认可——这是与获奖路线最实质的族内差异点 [H04]

---

## 四、顶层执行顺序（7 步正向推导 + 第 0.5 步数据形态直读）

**〔v1.4 新增·阶段时间预算表〕**（缺省按 72 h 赛程；实际按赛程长度等比缩放，并在第 1 步台账里登记「计划 vs 实际」）

| 阶段 | 占比 | 缺省预算 | 主要交付物 |
|---|---|---|---|
| 第 0.5–1 步 读题/定类/开台账 | ~10% | 7 h | 分类表 + MECH/INN 台账 + 口径标定表 |
| 第 2–3 步 假设与逐问建模 | ~25% | 18 h | 每问方法卡（决策点编号 + 溯源编号） |
| 第 4–5 步 求解与验证 | ~35% | 25 h | 数值结果表 + 图 + 验证协议 + 失败案例 |
| 第 6–7 步 自评/写作/收尾 | ~30% | 22 h | 论文 + 25 项自检清单 + scorecard |

> 预算纪律：任一步超出预算 50% 时先走【工程闸门】决定「降级交付 vs 追加投入」，禁止无声超支（[M]）。

**〔v1.5.2 新增·层间映射表（消主断点）〕** 本节 7 步是**主流程**；第二/三节是**取法来源**，
第五/六/七节是**支撑与控制层**。此前第五/六/七节在正文里从不被 7 步引用（唯一串联是文件头
『使用』一行），导致 `[U]`/`[Q]` 两层形同外挂。下表把调用关系钉死——**每一步都必须知道自己
要调哪一节的什么**：

| 步 | 本步做什么 | 取法来源 | 调用的配套/控制层 | 协作阶段 | 出口闸门 |
|---|---|---|---|---|---|
| 0.5 | 数据形态直读 | 第二节分类器 | — | 阶段0 | 数据形态表完成 |
| 1 | 问题重述 + 开台账 | 第二节分类器 | **第五节**（陷阱库 T1–T63 过一遍） | 阶段0 | MECH/INN 台账已开 |
| 2 | 建立假设 | 第三节各 STEP 的条件项 | — | 阶段1–3 | 假设清单 + 敏感性标注 |
| 3 | 逐问建模 | **第三节决策树**（A/B/B′/C/D/R/E/G 或 P/F/M/V/U） | **第七节**（接触点 ①②③：盲态方案 / 红队裁决 / 规格冻结） | 阶段1–4 | 建模规格冻结 + `口径表.md` |
| 4 | 求解 | 第三节 M/P 层 | **第六节 [U]**（需 GPU 则出任务包）+ **第七节**（接触点 ④ 看图） | 阶段5 | `registry.json` 有值 |
| 5 | 验证 | 第三节 D/E/V 层 | **第五节**（模板库 15 个）+ **第六节 [U]**（回传契约四件套） | 阶段5 | 数值核对三方一致 |
| 6 | 自评 | 第三节 U 层 | **第五节**（论文模板 25 项自检 + 图表证据角色表）+ **第七节**（接触点 ⑤ 数值红队 + 三盲评） | 阶段6 | scorecard 三席无 >20 分歧 |
| 7 | 工程化收尾 | — | **第七节**（接触点 ⑥ 缺口检查；台账收口 + `continuation_state`） | 阶段7 | 风险表 + 台账收口 |

> **两张进度视图的对齐**：上表左列是**解题者的步**（第二–四节），右列『协作阶段』是
> **三侧协作的段**（第七节）。两者不是同一套编号（**协作阶段刻意用「阶段N」，避开 §3.1 的 `S1–S5`**），
> 但必须同步推进：**协作阶段不能跑到解题步之前，也不能落后超过一个接触点**。

> **〔v1.1 工程外壳总览（新增；[M]=吸收 mathodology 流程层机制、[C]=对比实验实证）〕** 自 v1.1 起本流程叠加工程化外壳，构成『开题→收尾』闭环：开题（第 1 步内嵌 MECH 机制台账 + INN 创新台账，开台账）→ 分阶段执行（第 0.5–6 步，阶段之间由【工程闸门】卡放行）→ 收尾（第 7 步台账收口 + continuation state）。既有 6 步内容与编号原样保留，以下全部为增补。

**〔v1.1 新增·工程闸门〕（[M]，贯穿开题→收尾）**
- 解题显式分阶段：**问题理解 → 建模 → 求解验证 → 写作**，每阶段结束设独立 gate，未决 blocker/high 不放行（防跑偏、防无限打磨 [M]）；
- 严重度阶梯（[M]）：`blocker`＝违反赛规/破坏题意覆盖/模型失效/不可复现/提交不安全；`high`＝大概率降档（含结果呈现稀疏、论文优先题图表渲染缺陷）；`medium`＝应修复或显式接受风险；`low`＝不影响评分/正确性/复现/提交合法性的打磨项——blocker/high 未决不放行，medium 需有负责人/修复计划/显式接受理由，low 可排队；
- 迭代预算（[M]）：每 gate 修复 **≤2 轮**；整轮修复总预算 **≤8 轮**；任一预算耗尽**不静默继续**——输出 `decision_memo`（2–3 个选项 + 各自后果 + 推荐项）停下等人工决策；
- 无改进提前停：某轮未关闭 blocker+high 数未严格减少即停（改进=未关闭数严格减少 [M]）。

**第 0.5 步 数据形态直读（多数据集/多附件题强制）**
- 建模前逐数据集/附件读取随附**说明文件与表头**，确认每个字段形态：①类型体系/分类数 ②像素/字段值含义（类别标签/面积比例场/连续物理量/离散等级）③年份范围与截尾 ④缺测区域与坐标口径；**说明文件内容优先于常识猜测**；无法读取时输出【数据形态表】并显式标注不确定 [F02]；
- 连续比例场（如土地利用覆被比例 0~1 多波段）≠ 单一分类标签场：转移矩阵需 dominant 化（注意小类丢失）或比例转移两口径并存，建模前先钉死口径再动工 [F02]。

**第 1 步 问题重述（约 30 分钟）**
- 通读题面+参考文献标题：方法族聚类计数，**≥3 篇同族 → 该族即命题人暗示的主线**，纳入深度覆盖 [D01]；但只押一头是冒险（暗示 ≠ 评审唯一偏好），正确姿势是『理论主线+经典方法横向对比实验』两条腿走路 [D01]；
- 记录全部下载链接/数据集出处：链接可能是方法线索 [D01]；
- 严格按题面参数清单确定『合法信息集』：题面只给什么就用什么（D03 只用像距标量 a_i，与优秀论文一致），不脑补 [D03]；
- 列出题面全部数字并逐条翻译为口径/判废规则 [D05]；
- 题面公式为图片/OMML 无法文本提取时，**先做赛题 PDF/OCR 复原（复原优先）**；复原失败才走『文献标准形式实现+常数参数化（可切换开关）+标注【不确定】+以官方 PDF/更正说明为准』[F03]；
- 识别『建模需考虑 A/B/C 要素』类约束：先扫描两种解读（机理方程 vs 输入特征），竞赛优先『要素=输入特征』的数据驱动解读，物理机理作对照组 [D06]。

**〔v1.1 新增·第 1 步内嵌·机制台账 MECH〕（[M]，开题即开台账）**
- 开题**逐句扫题面**，把题面点名的每个现象/机制登记为三态之一：**建模**（进决策树取法）/ **代理**（给出代理方案）/ **降级**（给出理由）；显式标注高风险项（题面点名且直接决定评分的机制）；
- 收尾（第 7 步）**逐条收口 modeled / descoped**——**silent descope 是打分风险，不是简化** [M]；实证：2025C 同题对比中『卷绕同一性』（同一条裂隙跨展开图左右边缘会被数成两条，须跨边缘合并计数）正是经 MECH 台账登记才被 mathodology 版显式建模抓住，我方实战解答漏检 [C]；
- 第 0.5 步/第 1 步读出的『填充材质仅可代理』类题面点名项一律入台账并给代理方案，不静默忽略 [M]。

**〔v1.1 新增·第 1 步内嵌·创新台账 INN〕（[M]，开题即点创新）**
- 开题即点**至少一项『真实创新贡献』候选**：题面独有物理信息利用（如 2025C 卷绕同一性/展开图几何）、跨题迁移（异题同构技巧复用）、解析推导（公式级推导而非套库）等非显而易见贡献；无候选则显式标注『获奖天花板风险』，不伪装 [M]；
- 收尾收口：每条要么在正文**承重**要么显式 **descope**（写进局限）；**禁止把『合成数据还原生成族』或教科书做法当创新**——合成数据题以『恢复/匹配数据生成族』当头条贡献或选型理由被明令禁止，纯教科书应用最高只到国二档 [M]。

**第 2 步 建立假设**
- 三类假设显式声明：数据类（缺失/采样率/时区约定——相位基准最先统一 [D02][D07]）、约定类（口径表：状态阈值、片段定义、距离口径，开篇钉死、小问间完全复用 [D05]）、几何类（平面近似/针孔模型，用了就显式声明 [D02]）；
- 每个理想化假设标记『结论敏感性』：与空间位置耦合的假设优先级最高 [D03]；
- **被测量不做常识硬约束**：题面输出的量永远是可大值的输出对象，超常识区间只作『复核标点』软提示（拍照者高度 5.624m 案例）[D04]；
- 识别『可检验假设』：稳态/匀速类简化假设先默认出主结果、再显式检验成立性 [D04]。

**第 3 步 逐问建模**
- 按分类器给每个小问归类，进对应决策树逐点取法；
- 主动找范式转化机会（36 类→二分类 [D08]、先测速再测距 [D04]、分箱分类叙事 [D09]、残差学习 [D10]）；
- 注意递进结构：后问常复用前问内核（D03 Q1 内核=Q2 验证探针；D02 Q4 启示→Q3 插值超参数；D09 Q1 气象模型→Q4 消散修正）——打通小问是论文级联动亮点 [D03][D02][D09]；
- 开放/多事件题：深挖 1-2 个可获得验证数据的问题到『可检验准则+真实数据实证』级别，其余给统一框架 [D01]。

**第 4 步 求解**
- 优化题按模型形式定求解器层级：线性→LP、二次→QP、黑箱→GA/PSO/DE/贝叶斯（多起点）；目标约束对立补帕累托前沿 [D07]；
- 优先线性化/解析化：含内参未知数先消元、代入守恒约束降一维求根（证明单调性得全局唯一解）[D03]；极坐标参数线性化 [D02]；
- 反向优化类：前几问模型当黑箱代理嵌入目标/约束+启发式搜索（PSO/精英保留 GA）+数据范围定界（可扩张约 20%）+『至少 k 个标签达标』过滤（毒性标签方向取反：hERG=0/MN=0 才是好）；输出 Pareto 解集+区间表+**第二独立方法复核**+训练集回代 [D11]；
- 库设计类：组合库降元存储（C(n,2) 代 C(n,3)）+在线闭环校验+区间索引+物理可行性截断（角距≤视场对角线）[D03]；
- 优化方案型（给操作建议）：可叠 **DEA-BCC 标杆法**——DEA 有效样本均值构造『理想样本』作调整标杆（锚定真实运行工况、规避黑箱外推失真），与直接优化双轨互证 [D07]；
- 空间插值类（线状/稀疏观测场）：克里金/客观分析为主线（自带误差方差图）、IDW 仅作基线、可选几何感知构造（距离比法）、跨陆地/边界用掩模限制外推 [D02]。

**第 5 步 验证**
- **【数值强制交付】（v1.0 必改）**：即使数据附件缺失/禁网，也必须用合成/模拟数据（按题面规格：分辨率/时次/网格/题面常数；显式声明合成口径）**跑通完整管线**，交付带真实数字的结果表与图（预测表/误差表/参数表/拟合系数/敏感性曲线）；解题报告必须含数值型交付物，禁止以『预期实现结构』充当全部结果 [F03][F04]；
- 双闭环：内部一致性（免费真值：交叉点/前后段重估）+外部独立真值，误差分组归因 [D02]；
- 防泄漏：嵌套协议 [D07]、时间因果检查表+滚动重训 [D10]、公布真值只用于题目指定环节并写声明 [D08]；
- 无真值：三层自检（物理自洽/时间平滑/跨方法一致性）[D01][D04][D03][D09]；清洗审计表+『无需修改』证据 [D05][D07]。

**〔v1.1 新增·诚实性机制化〕（[M]，零编造纪律的机制化落地）**
- **by-construction 标注**：由构造/定义（重标定、归一化、硬上限）带来的收益必须显式标注『by construction』并报告真实代价，禁止当作发现的结果呈现 [M]；
- **headline-robustness**：头条数字（尤其『在蒙特卡洛/数值误差范围内压线达标』的约束结论）必须对其**最敏感参数**（含恢复得最差的参数）做压力测试，报区间或波动带 [M]；
- **CRN 共用随机数**：对比实验/对照策略沿用**同一随机数表**（common random numbers），比的是方法不是噪声 [M]；
- **概率结论报 MC 标准误**：概率类约束按实现频率报告，并附蒙特卡洛标准误 [M]；
- **未核实的信息不打印**：引用的页码/卷期/版本号只有在与一手出版记录核对确认后才可打印；无法核实的写【未核实】或省略 [M]。

**第 6 步 自评**
- 对照评审偏好自检：多模型对比是否在场 [D01][D05]；指标叙事是否可讲（识别率/混淆矩阵/分层误差）[D09]；是否报失败案例 [D03]；可视化是否齐全（轨迹着色/散点/混淆矩阵/xt 图/误差方差图）[D05][D11][D01][D02]；口径与换算常数全篇自查 [D05][D09][D02]；
- 逐小问自评把握度+不确定点清单（标注待核实标准/数据）——盲做阶段的『不确定自评』事后证明大多准确 [D05][D01]；
- 创新点/新概念必须与基准同指标对比，否则不构成证据 [D05][D06]；开放题主动写局限（写局限是加分项）[D11]。

**〔v1.1 新增·三盲评自评协议〕（[M]，交稿前最后一道自评）**
- 交稿前以**三个评审视角自评**（互相独立、不看彼此打分），每席给分项评分表 + 一句 **fix_one_thing**（『只修一件事』是什么）：
  - **席 A＝综合型评委**：综述（摘要）/ 建模 / 结果；
  - **席 B＝创新与决策可用性评委**：创新 / 证据 / 写作；
  - **席 C＝怀疑派应用数学审稿人**：正确性 / 复现性（权重最重，怀疑派立场）；
- **三席共享项（综述/建模/结果）分歧 >20 分＝证据冲突**：回查两席引用的证据、只复核离群席一次，**重新审查而非平均**——冲突未解决前不通过 [M]；
- 档位阈值**参考锚点（参考锚点不是承诺）**：国一 总分 ≥85、单项下限 70；国一边缘 80/65；国二 75/60；通过条件＝三席隐含档位达标 + 最低总分过线 + 无单项低于下限 + 无未解决 >20 分歧 [M]；
- 实证：对比实验中三席 weighted_total 均 <58、fix_one_thing=『跑通全链』——模拟评委后自我认知更清醒，比无协议自评苛刻得多 [C]；具体表格见论文模板《评审模拟 scorecard》。

**第 7 步 工程化收尾（v1.1 新增，[M]）**
- **台账收口**：MECH 逐条收口 modeled / descoped；INN 逐条正文承重或显式 descope——不留悬空条目（对照第 1 步开户）；
- **continuation state（多会话续跑轻量格式）**：每轮结束输出六字段——`当前阶段 / 已过闸门 / 待决问题 / 假设清单 / 产物路径 / 下一步`，供下一会话（或人工）不重读完整历史即可续跑；结构化输出，禁止自由文本式交接 [M]。

---

## 五、配套文件（路径说明）

以下文件与本文件同目录（整包安装为一个技能文件夹后，相对路径即当前目录；`**模板库**` 内含 README.md 索引）：
- **陷阱库**：`陷阱库.md` —— 共 **64 条**偏离点浓缩（T1–T64 连续），分 8 批：D 系列 T1–T30（v0.1 蒸馏 11 题）、H 系列 T31–T42（v0.9 盲测 4 题）、F 系列 T43–T48（v1.0 实战 4 题）、T49–T50（v1.1 流程层 [M][C]）、T51–T53（v1.2 实测实证层 [E]）、T54–T57（v1.3 无标注实战层 [G]）、T58–T61 + T62–T63（v1.5 多目标检测与定量测量层 + 工程实现类 [G]）、**T64（v1.6 响应提取与加权拟合类 [G]）**。每条格式：陷阱（一句话）→ 后果 → 规避规则 → 溯源（D01–D11/H01–H04/F01–F04）；交稿前逐条过一遍。
  > **v1.5.2 更正**：① v1.4/v1.5.1 正文曾误记『共 53 条』，与实际 63 条不符；② 原『历次追加』表述**漏记 v1.3 整批（T54–T57）**，账面 48+2+3+6=59 对不上 63，缺口正是这 4 条；③ 层列表漏列『无标注实战层 / 多目标检测与定量测量层 / 工程实现类』。以上均按 `陷阱库.md` 实测复核改正。
  > **账目（v1.6 闭合）**：30 + 12 + 6 + 2 + 3 + 4 + 6 + **1** = **64**，由 `tests/audit_assets.py` 自动对账。
- **论文报告结构模板**：`论文报告结构模板.md` —— 对照华为杯评阅习惯的章节结构（问题重述/模型假设/符号说明/建模与求解/检验/评价）+ 提交前可勾选自检清单；v1.1 新增《图表证据角色表》（逐图登记证据角色，禁装饰图）与《评审模拟 scorecard》（三席自评+fix_one_thing+分歧复查）两节（[M]），交稿前与自检清单一并跑完。
- **代码模板库**：`templates/` —— 可直接改写的 Python 函数骨架，用前先看 `README.md` 的『模板×环节』索引与管线编排顺序：
  - `image_preprocess.py`：图像类预处理（帧配准、RPCA/背景减除、暗通道去雾、信号滤波等）
  - `image_features_modeling.py`：图像类特征/模型/评估（不变量、姿态解算、交比测高、识别漏斗、能见度估计）
  - `predictor_data_audit.py`：预测类数据审计（缺失/异常/口径/时间对齐/分布检查）
  - `predictor_feature_engineering.py`：预测类特征工程（相关性筛选、合成指标、WOE/IV、窗口特征）
  - `predictor_modeling_validation.py`：预测类建模与验证（基线对比、防泄漏嵌套协议、不均衡处理、Stacking、同构不同参分训）
  - `uncertainty_optimization.py`：不确定性分析与约束优化（残差/区间、Sobol 灵敏度、代理模型+PSO、可行域定界）
  - `signal_processing.py`：阵列/谱估计（MDL/AIC 定阶、MUSIC/ESPRIT/root-MUSIC、解相干、自校准 ALS、CS，[H01]）
  - `emissions_ledger.py`：双碳核算矩阵（部门×品种×因子、勾稽校验、数据缺口标注，[H02]）
  - `scenario_planner.py`：情景规划闭环（政策锚点参数化、对标判定、OAT 灵敏度、情景对比表，[H02]）
  - `clinical_longitudinal.py`：临床纵向建模（LME、患者级 CV、有序累积链接+QWK、IPW 混杂控制，[H03]）
  - `ensemble_forecast.py`：集合预报（多成员生成/幅度方向扰动、P10/P50/P90 分位聚合、区间输出、平流一致性检查，[F04]）
  - `polygon_to_mask.py`：分割标注转换（YOLO/COCO 多边形→二值 mask 双口径、正样本像素占比与 mask 覆盖率一致性检查，[E]）
  - `segmentation_eval.py`：分割评估（逐图 Dice/IoU/P/R、分布分位数、批级 vs 逐图口径、最差样本三联图导出，[E]）
  - `sinusoid_hough.py`：参数化曲线 Hough 搜索（题面公式即主模型、粗到细搜索、垂直容差带打分、迭代贪心+邻域抑制去重、图内自适应阈值、物理标定优先；对应 STEP G2/G3/G4，[G]）
  - `multi_target_dedup.py`：多目标去重与归并（观测空间重叠度 + 参数距离的合取判据、连通分量聚类；只做**归并**这一步，**枚举**由 `sinusoid_hough.py` 的邻域抑制承担；对应 C5/C6，[G]）
- **自检与清点**：`tests/`
  - `smoke_test.py`：模板冒烟测试（AST 全量语法检查 + 两分割模板端到端 + **结构断言**）；无需网络与真实数据
  - `audit_assets.py`：**资产清点脚本**——对账「决策点 / 子条件 / 陷阱编号连续性 / 模板数」，
    并交叉校验 `SKILL.md` 头部『资产清点』与 `README.md` 表格是否相等。
    **本文件头部『计数纪律』所称的「清点脚本」即指它**；不符即非零退出码，可直接挂 CI。
    用法：`python tests/audit_assets.py`（`--quiet` 只报结论）
- 使用顺序：本文件定方法论与执行顺序 → 解题中对照陷阱库防坑 → 代码骨架套 templates/ → 写作套论文模板 → **提交前跑 `python tests/smoke_test.py`（含清点对账）**。

---

## 六、算力与交付形态〔[U] 环境约定层，非题源方法论〕

> **层性质声明（防污染）**：本节**不是**从任何赛题蒸馏出的结论，**不参与 D/H/F 溯源**，也**不得据此修改第三节决策树的任何决策点**。它记录的是**当前工作条件**，只起两个作用：① 选方法时**不再受本机资源约束**；② 交付物多做一份「可直接上传即跑」的任务包。条件变化（换平台/换账号/本地升级）时**整节替换**，不牵动其余章节。

### 6.1 工作条件（用户约定 2026-09-13）

- **深度学习可随意使用**：数学建模中不再因为「本机 RTX 3060 Laptop 6GB 跑不动」而主动降级方案；
- **算力来源 = 用户本人的 AutoDL 账号**（租用 GPU 实例，按量计费）；
- **三方分工**：助手**把需要的数据与程序全部准备好**（可上传即跑）→ **用户在算力平台执行** → **用户回传结果** → 助手据回传结果出数字、图与正文。

由此三条纪律（**顺序不能反**）：
1. **先按题面最优方案选型**，资源不匹配时才考虑工程降级——不是先看显卡再定方案；
2. 任何需要 GPU 的方案，**同时给出「本地能否跑」的判断**——本地能跑就本地跑，省上传与排队的时间；
3. 远端执行**看不见过程** ⇒ 任务包必须自带日志与自检，**禁止依赖「跑的时候我在旁边看着」**。

### 6.2 任务包规范（助手的交付物形态）

一个任务包 = 一个目录，用户打包上传后**一条命令开跑**：

| 文件 | 作用 | 硬要求 |
|---|---|---|
| `run.sh` | 唯一入口 | 幂等可重跑；开头打印环境自检（GPU 型号/显存/驱动/python 版本） |
| `requirements.txt` | 依赖 | **一律 pin 版本**（`==`），不写 `>=` |
| `main.py`/`train.py` | 主程序 | **路径全参数化**（`--data-root` / `--out-dir` / `--seed`）；**不得出现本机绝对路径** |
| `README.md` | 操作说明 | 3 步内跑通（传到哪→装什么→跑哪条命令）；写明**预期耗时、峰值显存、产物清单** |
| `data/` 或 `data/README.md` | 数据 | 大数据集**不进包**，走网盘/SCP；包内只放小样例 + 放置说明 |
| `results/` | 输出 | 见 6.3 回传契约 |

**易犯硬约束**：写死 `C:\Users\...`/`G:\...` → 相对路径 + 命令行参数；只往 stdout 打印就算完 → 同时落 `run.log`（完整命令、环境、耗时、峰值显存）；忘记固定种子 → `--seed` 默认固定并在结果里**回写 seed**；长任务不能续跑 → 写 checkpoint + `--resume`；**产物路径含中文/空格 → OpenCV 的 `imwrite` 会静默失败**（返回 `False` 而不抛异常，图"没保存"却无报错，远端排查极贵）——优先用 ASCII 路径；确需非 ASCII 路径时，统一走 **`imwrite_u`**（`sinusoid_hough.py` 内已提供：`cv2.imencode(...)[1].tofile(path)` + **落盘后校验文件存在且非空**，不信返回值、只看产物）。对应陷阱库 **T63**，本处为其在交付链上的落点。

### 6.3 回传契约（用户 → 助手，**四件缺一不可**）

| # | 回传物 | 为什么必须 |
|---|---|---|
| 1 | `results.json` | **机器可读**，直接喂 registry（数值口径唯一事实源） |
| 2 | `run.log` | 完整命令 + 环境 + 耗时 + 峰值显存 + 报错原文；**远端唯一的过程证据** |
| 3 | 图/表产物（png/csv） | 进正文与 PPT |
| 4 | `env.txt` | `python -c "import torch,sys;print(sys.version,torch.__version__,torch.version.cuda,torch.cuda.get_device_name(0))"` + `nvidia-smi` 摘要 |

> 回传前**不要手工改数字**：正文数字一律由 registry 从 `results.json` 取，改了必然对不上（T62 就是手写数字与产物不一致）。

### 6.4 AutoDL 实操要点（官方文档核对，2026-09-13）

| 项 | 事实 |
|---|---|
| 数据盘 | `/root/autodl-tmp`，**50GB 起、可扩容**，本地盘快，**关机不丢失**，但不能保存进镜像 |
| 大文件传输 | 走**公网网盘**（百度/阿里云盘）双向；从网盘下载**固定落到 `/root/autodl-tmp`** |
| SCP/Rsync | 端口是**映射端口，不是 22**：`tar cf - * \| ssh -p <端口> root@<region>.autodl.com "cd /root/autodl-tmp && tar xf -"` |
| 学术加速 | `source /etc/network_turbo`（加速度 github.com / huggingface.co）；**用完 `unset http_proxy https_proxy`**，否则干扰正常网络 |
| HF 缓存 | `export HF_HOME=/root/autodl-tmp/cache/`，否则撑爆系统盘 |
| 计费 | 按量计费，**跑完及时关机**；关机后数据盘数据保留（细则以控制台为准【不确定】） |

**铁律**：**系统盘容量小**——数据集、中间产物、checkpoint、HF 缓存**一律指向 `/root/autodl-tmp`**。

### 6.5 深度学习不是免罚金牌（与第三节的衔接，均为既有规则的复用）

- **DL 可用 ≠ 可以不做对比**：「多模型对比是得分点」不变，DL **必须与基线/经典方法同协议对比**（同数据、同划分、同指标口径）；
- 评估口径沿用 **STEP E** 纪律（逐图 vs 批级口径、Dice 与 IoU 双报、报最差样本）；
- 报 DL 结果**必须带训练成本**（卡型/耗时/峰值显存）——远端跑得动 ≠ 方案可复现；
- **不为用 DL 而用 DL**：数据量不足时优先迁移学习或传统方法，并在正文说明理由（这本身就是得分点）。

### 6.6 三侧接口

> **v1.5.2 起本节只作索引**：完整的三侧分工、权威划分、通道配方、核对协议与裁决阶梯
> 已独立为**第七节〔[Q] 三侧协作与核对层〕**（它跨全程，不属于本节的算力/交付语义）。
> 本节保留一句总纲：
>
> **Qwen 侧 = 建模与数据权威；助手侧 = 实现与账本权威；用户侧 = 终审权威。**
> 唯一**执行数值**事实源 = `registry.json`；唯一**口径与常数**事实源 = `口径表.md`。
> 两个账本分离记账，是三侧协作防漂移的核心机制（详见 7.6）。

---

## 七、三侧协作与核对层〔[Q] 流程层，非题源方法论〕

> **层性质声明（防污染）**：与第六节同性质——本节**不是**从任何赛题蒸馏出的结论，
> **不参与 D/H/F 溯源**，**不得据此修改第三节任何决策点**。它规定的是
> 「Qwen（建模侧）/ 助手（实现侧）/ 用户（终审侧）」三侧如何分工、如何互相核对、
> 数值权威如何归属、口径漂移如何防护。**通道或约定变化时整节替换**，不牵动其余章节。

### 7.1 三侧分工与权威划分（v1.5.2：数据权威移交 Qwen）

| 侧 | 负责 | 权威域 | 禁止 |
|---|---|---|---|
| **Qwen（建模侧）** | 判据、方案取舍、看图、口径定义、模型选型、**常数与阈值取值**、红队质疑、创新点评估 | **建模权威 + 数据（口径/常数）权威** | 不写可执行代码；不替代账本记账 |
| **助手（实现侧）** | 分类定类、决策树取法落地、代码实现、`registry.json`、图、交付包与**论文正文** | **实现权威 + 账本权威** | 不擅自改写 Qwen 已定的口径与常数；**不替用户下单/操作账号** |
| **用户（终审侧）** | 给题与数据、算力执行、回传四件套、终审、done 判定 | **终审权威** | 不手工改回传数字 |

> **论文正文归属**（v1.5.2 补记，防止 §6.6 改写后责任悬空）：**方法与数值由本层三侧协作产出，
> 论文正文成文由助手负责**。本层只覆盖「题目 → 数据与方法」，**不代写正文论述**；
> 正文按 `论文报告结构模板.md` 在第四节第 6 步完成。

> **v1.5.2 关键变更（解除禁令）**：v1.5.1 及以前规定「Qwen 侧**不输出任何具体数值**」。
> 自 v1.5.2 起该禁令**解除**——用户指令：严格按「**Qwen 建模 + 助手编程**」分工，
> **数据权威移交 Qwen 侧**。Qwen 可以且应当给出模型常数、物理阈值、参数取值、
> 口径定义与判据边界等**方法论层数值**。
> **随之迁移的风险**：原禁令靠「不让它说数字」防漂移；解除后防漂移改由
> **双账本 + 依据来源标注 + 口径冻结**承担（见 7.6），**不是取消防护**。

### 7.2 通道：Qoder CN CLI（实测配方）

Qwen 侧通道 = `qoderclicn`（Qoder CN CLI，非 `qwencn`），模型 `Qwen3.8-Max`；
走用户既有 Qoder 订阅，**无需独立 API Key**。适配器脚本 `qwen-call.ps1` 与
协议全文见工作目录 `huawei-cup-workflow/`。

```powershell
& qoderclicn.exe --tools "" -p -o json -w <工作目录> -m "Qwen3.8-Max" `
                 [--session-id <uuid> | -r <uuid>] [--attachment <file>] < 提示词走 stdin
```

**六个实测坑（改动前必读）**：

1. **`--tools ""` 必须放最前**——它是变长参数，位置错会把 query 当工具名吞掉 →
   **exit=0 但零输出**（最危险的静默失败）。适配器对此显式判错。
2. **`--` 分隔符不可用**（实测 exit=42）。
3. **会话按项目目录存储**，resume 必须用与首轮相同的 `-w`，否则报 `Invalid session identifier`。
4. **长中文提示词走 stdin**，不要拼进命令行（Windows 长度与引号双重风险）。
5. **`-o` 只接受** `text` / `json` / `stream-json`。
6. **`total_credits` 疑为会话累计值**（同会话三轮递增、独立会话无关）——按差分读单轮成本，
   该疑点标【不确定】，未核实前不得据此做预算硬约束。

### 7.3 七阶段 × 六接触点

```
阶段0 题目入库（助手）  0.5步数据形态直读 + 分类器定类 + 小问拆分
        │              产物: 00_intake/
        ▼
阶段1 盲态独立方案 ──────────────────────────── 接触点 ①（新建会话）
   助手: 决策树取法 → 01_blind/我方方案.md
   Qwen: 同题面+同数据形态表 → 01_blind/qwen方案.md      ★ 双方互不可见
        ▼
阶段2 交叉比对（助手）→ 02_diff/分歧台账.json  一致项 / 分歧项 / 单侧独有项
        ▼
阶段3 分歧裁决 ──────────────────────────────── 接触点 ②（续跑 ①：红队反驳）
        │  产物: 03_adjudicate/裁决记录.md
        ▼
阶段4 建模规格冻结 ──────────────────────────── 接触点 ③（续跑 ①：复核规格）
        │  产物: 04_spec/{建模规格_冻结.md, 口径表.md, 变更记录.md}   ★ 冻结后改动走变更记录
        ▼
阶段5 实现与数值产出（助手）
        │  本地可跑→本地；需 GPU→AutoDL 任务包        ◄─ 接触点 ④（新建会话：看图）
        │  产物: 05_impl/{code/, registry.json, results/, figures/}
        ▼
阶段6 双层核对
        │  层1 数值核对（机器，无模型）: registry ↔ 表 ↔ 图 三方一致
        │  层2 方法核对 ──────────────────────── 接触点 ⑤（续跑 ②：结果红队）
        │     + 三盲评 scorecard（第四节第 6 步）       产物: 06_verify/
        ▼
阶段7 交付包 ────────────────────────────────── 接触点 ⑥（新建会话：换视角查缺口）
        │  产物: 07_deliver/{交付包.md, 风险表.md, continuation_state.md}
        ▼
   用户终审（用户确认才 done）
```

> **编号避碰（v1.5.2）**：本层的阶段编号用 **「阶段0–阶段7」**，**刻意不用 `S0–S7`**——
> 因为 §3.1 STEP B′ 的谱估计决策点已占用 **`S1–S5`**（定阶/超分辨/解相干/稀疏重构/自校准），
> 两者同处一个文件，若都叫 `S1` 必然在引用时串号。旧稿与协议曾用 `S0–S7`，详见 CHANGELOG。

**会话编排纪律**：盲态轮、看图轮、换视角轮**必须新建会话**；同一方案的连续质疑才续跑。
混用会同时毁掉独立性与上下文效率（实测第 3 轮 `context_usage_ratio` 已升至 0.038）。

**通信预算分层**（决定「哪些环节要占一次 Qwen 轮次」）

> **模型纪律（v1.5.2 修订）**：Qwen 侧**只调用 `Qwen3.8-Max`，不存在第二档模型**。
> 本层分的是「**这件事该由 Qwen 建模，还是由助手自己干**」，**不是「发哪个模型」**。
>
> **分工定性**：**Qwen3.8-Max 负责建模判断（判据、口径、方案取舍、看图、公式复核、失败归因、
> 红队质疑、创新点评估）；助手负责编程与其余一切细碎事项**——备数据、供证据、跑实验、记账、出图、
> 调试、格式化。**凡涉及建模判断，一律交给 Qwen3.8-Max；凡属工程杂事，助手自己做，不占用 Qwen 轮次。**

- **交给 `Qwen3.8-Max`（建模判断，占 Qwen 轮次）**：定类仲裁、小问拆分歧义、口径选择、
  方法族取舍、**看图（`--attachment` 传图）**、**公式复核**、**失败案例归因**、红队质疑、
  创新点评估、数值红队；
- **助手自己做（编程与细碎事项，不占 Qwen 轮次）**：代码实现、调试、数值计算、画图制表、
  文件组织、格式整理、数据搬运。其中**产出将被 Qwen 复核的结论**（数值表、图、口径表草稿），
  **复核是出口条件，不是可选项**——助手不得自行签发建模结论。

### 7.4 反谄媚核对协议（[Q] 核心机制）

问对方「我这个对吗」必然得到「对」。有效核对必须同时满足三条：

1. **盲态先做**（阶段1 双方互不可见）——分歧才有信息量；
2. **强制红队**（只许反驳，不许赞同）；
3. **裁决阶梯**（7.5）——能实证的用实验裁决，**不平均、不投票、不和稀泥**。

**强制红队轮模板**（实测有效，直接可用）：

```
以下是一个方案的完整依据。你的任务**只是反驳**。
规则：禁止输出任何赞同性评价。
若你确实找不到反驳，输出「未发现反驳」，并列出你尝试过的 3 个反驳方向。
只输出 JSON：{"反驳":[{"点":"","依据":"","需要的证据":""}],
             "尝试过的方向":[""],"未发现反驳":false}
```

> 实测效果：对同一方案给出 6 条实质反驳（Jensen 不等式致倒数反演有偏、越权注入领域惯例、
> 无数据支撑的协变量、留出验证的信息泄漏、稳健统计偷换估计目标、窗口划分起点未定），
> **通篇零赞同语**，并如实报告「尝试过但未找到反驳的 3 个方向」。

**附加纪律**：
- **双向自红队**：助手给自己的方案写红队，Qwen 给它的方案写红队 → 交换 → 再交叉红队；
- **红队轮不给自己的推荐**：只给依据、不给结论，避免它顺着推荐找理由；
- **数值红队（v1.5.2 补）**：红队轮除方法质疑外，必须对 `口径表.md` 中**每个 spec 数值**
  追问「`依据来源` 是否属实」，并要求**逐条复述依据 + 给出一个反例场景**。
  理由：把 `推测` 写成 `领域知识` 是本层**最难被发现的失效模式**——它形式合规、
  无人核对，却直接抬升该值的裁决优先级（§7.6 序 3）。**没有数值红队轮，双账本的可信度无从保障。**
- **未关闭分歧一律进风险表**，不允许从交付包消失。

### 7.5 分歧裁决阶梯（自下而上，先用低层）

| 层 | 裁决方式 | 适用 |
|---|---|---|
| L1 | **跑实验** | 可实证的分歧——写最小脚本，**数值说话。永远优先** |
| L2 | **回读题面与附件说明** | 题面/说明文件已写死的口径 |
| L3 | **对照第三节决策树与陷阱库** | 已有 D/H/F/E/G 溯源支撑的取法 |
| L4 | **查文献/领域共识** | 助手联网核实，附 URL |
| L5 | **双轨保留** | 无法裁决 → 两条路线都实现，**同数据/同划分/同指标**对比（这本身即评分点） |
| L6 | **decision_memo 交用户** | 2–3 个选项 + 各自后果 + 推荐项，停下等人工决策 |

**禁止**：投票平均、和稀泥、把分歧默默写成「两种说法都存在」。

### 7.6 数值权威与防漂移（双账本，[Q] 的核心防护）

解除「Qwen 不输出数值」禁令后，数值分两层、两本账、两套权威：

| 层 | 内容 | 权威 | 账本 | 交付时 |
|---|---|---|---|---|
| **spec 层**（方法论数值） | 模型常数、物理阈值、参数取值、口径定义、分箱边界、判据阈值 | **Qwen** | `口径表.md`（`来源=Qwen`） | 直接采信 |
| **result 层**（执行数值） | 由实际数据算出的结果：均值/Dice/RMSE/拟合参数/预测值 | **代码产出** | `registry.json` | 必须可追溯到脚本+命令+种子 |

**四条铁律**：
1. **spec 层每个数字必须带 Qwen 的 `依据来源`**（`题面`/`领域知识`/`文献`/`推测`）——
   实测 Qwen 会如实填写。该字段**不是标注，是优先级**（见下「裁决优先级」）；
2. **result 层不经代码不得成立**——Qwen 若给出结果类数值，标记为 `预估值`，
   **只用于交叉合理性检查，不作为交付数字**；并**禁止以预估值反向驱动调参**
   （反例：Qwen 估「Dice 应 ≥0.8」→ 助手把实测 0.74 调到 0.8，交付数字仍出自代码、
   形式合规，实质却被不可核实的估计牵着走。**预估值不得进入任何目标函数或早停判据**）；
3. **两本账分离记账，边界要可执行**——`口径表.md` 存**口径的取值与其定义**；
   `registry.json` **只存执行结果**，其「口径」字段**引用口径表的键、不得复制取值**
   （写 `"口径": "口径表#MOR-eps"`，而不是 `"epsilon=0.05"`——后者按本条即缺陷）。
   *边界例外*：E1/T51 要求的「正样本像素占比」这类**既是过程口径又是执行结果**的量，
   一律按 **result 记账**并回填口径表引用，**不重复赋值**；
4. **口径表在阶段4冻结**，冻结后任何改动走**变更记录**（`04_spec/变更记录.md`：改了什么 /
   为什么 / `依据来源` / 影响哪些小问 / **签发人**）。**现实口径**：阶段4能冻结的实际只有
   **题面级常数与口径定义**；阶段5实测必然暴露需改的参数（如 T56「固定阈值致 4/10 张
   零检出」），此时**走变更记录，不算违反冻结**——把变更当违规，只会逼出"交付已知失效管线"
   或"偷偷改口径"两种更坏的结果。

**裁决优先级（消解「spec 权威」与「实验优先」的冲突）**：

> §7.5 的 L1「跑实验永远优先」**适用于一切可实证的分歧，spec 层也不例外**；
> 「以 Qwen 为准」**只在不可实证时才生效**。完整优先级：

| 序 | 判据 | 说明 |
|---|---|---|
| 1 | **题面明给** | 题面/附件说明写死的数字与口径——**最高**，Qwen 与助手均不得覆盖 |
| 2 | **可实证** | 跑最小实验裁决（§7.5 L1）。**「ω 取 0.95 还是 0.85」这类取值属此列**，不得因"属 spec"就免于实验 |
| 3 | **Qwen 依据来源分层** | 不可实证时，按 `领域知识` > `文献` > `推测` 取 Qwen 值；`推测` 项须在正文标注不确定性 |
| 4 | **助手实现约束** | 仅当该值工程上不可实现时提出异议，且须回报 Qwen 复核 |
| 5 | **仍不决** | §7.5 L5 双轨保留 → L6 decision_memo |

**跨层冲突**（spec 值 vs 实测量级）**一律走序 2**：**实测优先于领域惯例预期**。
反例：Qwen 依领域知识给「尺度曲线 N≥100 收敛」（spec），实测 80 即收敛（result）→ 以实测为准。

**冲突记账**：spec↔spec 按序 1–3 裁决并记录理由；result↔result 以 registry 为准并重跑复核。
**任何裁决都写进变更记录，不留口头结论。**

**与第三节的关系（例外条款，防本层越权）**：第三节决策点中**已出现的常数**
（如 ω≈0.95、暗通道最亮前 0.1%、early-stop patience=8、共线性 |r|>0.9、门控 softmax>0.8、
判定阈值 0.5、集合扰动 ±10–20% 等）**视为题面级，属上表序 1**——Qwen **不得覆盖**，
只能在题面未给时**补充定义**。据此，本层「不得修改第三节任何决策点」的约束继续成立。

### 7.7 通信台账与降级

每轮 Qwen 调用追加一行到 `transcript.jsonl`：`ts / phase / model / session_id / resumed /
prompt_sha / attachments / wall_s / api_ms / credits / context_ratio / schema_used / result`。
这张表是「时刻保持通信」的**可审计替代物**，阶段7 时可直接作为协作过程附录。

| 情形 | 处置 |
|---|---|
| Qwen 通道不可用 | **不阻塞**：助手按第三节决策树独立走完 S2–S7，交付包标注「未经 Qwen 复核」+ 风险表登记 |
| 契约返回 exit≠0 | **停下报告**，不重试、不降级续跑 |
| exit=0 但零输出 | 判定为 `--tools ""` 位置错，按 7.2 坑 1 修正后**只重试一次** |
| 单会话 `context_usage_ratio` >60% | 强制换新会话 |
| 裁决到 L6 仍不决 | 发 `decision_memo`，暂停相关小问，其余小问继续 |
| credits 逼近额度上限 | **不降级模型**（建模判断不因额度降档）。改为**压缩接触点**：合并 ③④ 为一次、砍 ⑥，优先砍 T2 类复核；同时发 `decision_memo` 报用户 |
